# 支持残差连接网络设计修改思路

## 1. 目标

在当前 cnn-noxim 中支持 ResNet 类残差连接网络的仿真，使其能够表达如下结构：

```text
branch input
   |----------------------|
   v                      |
 Conv/BN/ReLU -> Conv ----+-> Add -> ReLU -> next layer
```

核心目标不是只得到数值结果，而是让残差分支的数据也真正通过 NoC 发送、接收、恢复并参与计算，这样通信时间、flit 数量、网络能耗和近似通信效果才有意义。

## 2. 当前 cnn-noxim 的限制

当前工程基本假设网络是线性结构：

```text
Input -> Conv/Pool/FC -> Conv/Pool/FC -> ...
```

现有代码中很多地方默认数据只从第 `ID_layer - 1` 层流向第 `ID_layer` 层，或者从第 `ID_layer` 层发送到第 `ID_layer + 1` 层。主要体现在：

1. `NNModel::load()` 解析模型文件时，只支持 `Input`、`Convolution`、`Pooling`、`Dense` 等顺序层。
2. 坐标映射和通信缓存预计算时，主要围绕“当前层到下一层”的关系生成发送表和接收表。
3. `NoximProcessingElement::reset()` 中，每个 PE 的 `receive`、`receive_Neu_ID`、`should_receive` 基本由前一层决定。
4. 接收端虽然可以按 `src_Neu_id` 匹配数据，但目前没有“一个层接收来自两个或多个前驱层”的模型描述。
5. 计算阶段没有 element-wise add 这种层类型。

因此，直接使用 ResNet-8 这类网络时，最大问题不是卷积计算本身，而是残差分支无法在模型层级和通信层级被表达。

## 3. 推荐总体方案：新增显式 Add 层

推荐新增一种显式层类型，例如：

```text
Add
ResidualAdd
```

它作为普通网络层参与映射、通信和计算。

示例结构：

```text
Layer 1: Conv
Layer 2: Conv
Layer 3: Add, input from Layer 2 and Layer 0
Layer 4: Conv
```

这样残差连接就被转换为一个普通的数据流图：

```text
Layer 0 -> Layer 1 -> Layer 2 -> Layer 3
Layer 0 -----------------------> Layer 3
Layer 3 -> Layer 4
```

这种设计的优点：

1. 可以最大程度复用现有 flit、packet、PE 接收、近似通信和统计逻辑。
2. 残差分支的数据会真实经过 NoC，通信开销不会被隐藏。
3. `Add` 层之后的输出可以继续作为普通层输出，被 FAS、ABDTR、zero-skip、SAP-RLE、SAP-RLE-V2 等方案处理。
4. 对旧的 AlexNet、VGG、LeNet5 等线性网络影响最小，因为没有残差边时仍然退化为原来的顺序结构。

## 4. 模型文件格式设计

可以在现有模型文件格式中新增 `Add` 层描述。

建议格式之一：

```text
Add H W C src_layer_0 src_layer_1 act_fun
```

含义：

| 字段 | 含义 |
|---|---|
| `Add` | 层类型 |
| `H W C` | 输出特征图尺寸 |
| `src_layer_0` | 主分支来源层编号 |
| `src_layer_1` | shortcut 分支来源层编号 |
| `act_fun` | 加法后的激活函数，例如 `none` 或 `relu` |

例如：

```text
Add 32 32 16 4 1 relu
```

表示当前 Add 层接收第 4 层和第 1 层的输出，做逐元素相加，然后经过 ReLU。

更通用的格式可以扩展为：

```text
ResidualAdd H W C src_num src0 src1 ... act_fun
```

这样未来可以支持超过两个输入的 element-wise add。

## 5. 数据结构修改思路

当前代码可以继续保留 `all_leyer_type`、`all_leyer_size`、`Group_table`、`mapping_table` 等结构。

建议新增一份“层输入来源表”：

```cpp
all_layer_input_layers[time_div_mul][layer_id] = {source_layer_ids...};
```

对于普通线性层：

```text
Input: 无前驱
Conv:  {layer_id - 1}
Pool:  {layer_id - 1}
Dense: {layer_id - 1}
```

对于残差 Add 层：

```text
Add: {main_branch_layer, shortcut_layer}
```

这样可以把原来的“默认前一层”逻辑，逐步改成“读取当前层的输入来源”。旧模型不写残差连接时，解析器自动补成 `{layer_id - 1}`，不影响旧功能。

## 6. Add 层映射方式

Add 层本身没有权重和偏置，只有逐元素计算。

如果输出特征图为：

```text
H * W * C
```

则 Add 层的神经元数量就是：

```text
Use_Neu = H * W * C
```

可以沿用现有分组和映射逻辑：

1. 按 `groupsize` 把 Add 层输出神经元分组。
2. 分配到 PE。
3. 每个 PE 负责一部分 Add 输出。
4. Add 层计算完成后，其输出写入 `res[pic][local_neuron_id]`。
5. 后续层把 Add 层当成普通前驱层读取。

这样可以最大程度复用当前卷积层、池化层和全连接层的 PE 管理方式。

## 7. 通信缓存修改思路

这是支持残差连接最关键的部分。

当前通信缓存主要默认：

```text
当前层 -> 下一层
```

残差网络需要支持：

```text
当前层 -> 一个或多个后继层
```

建议在 `NNModel::load()` 中模型解析完成后，根据 `all_layer_input_layers` 建立反向消费者表：

```cpp
layer_consumers[src_layer].push_back(dst_layer);
```

例如：

```text
Layer 0 -> Layer 1
Layer 1 -> Layer 2
Layer 2 -> Layer 3
Layer 0 -> Layer 3
```

则：

```text
layer_consumers[0] = {1, 3}
layer_consumers[1] = {2}
layer_consumers[2] = {3}
```

通信预计算时，不再只看 `ID_layer + 1`，而是遍历当前层的所有 consumer layer：

```text
for each src_layer:
    for each dst_layer in layer_consumers[src_layer]:
        生成 src_layer -> dst_layer 的通信关系
```

对于普通线性 CNN，消费者表等价于原来的下一层，所以旧模型行为应保持一致。

## 8. PE 接收端修改思路

当前 PE 接收逻辑本身有一个优点：它是通过 `src_Neu_id` 匹配数据的。

因此，如果 Add 层的 `receive_Neu_ID` 正确包含两个来源层的神经元 ID，接收端不需要大改。

对于 Add 层，初始化时应设置：

```text
receive = source_count * Use_Neu
```

如果 Add 层有两个输入分支：

```text
receive = 2 * Use_Neu
```

并构造：

```text
receive_Neu_ID = [
    src0 对应位置的全局神经元 ID,
    src1 对应位置的全局神经元 ID
]
```

当两个分支的数据都到达后：

```text
should_receive[pic] == 0
```

Add 层开始计算。

这能复用现有的等待机制，不需要额外写复杂的同步状态机。

## 9. Add 层计算逻辑

在 `NoximProcessingElement.cpp` 的计算分支中新增一种类型，例如：

```cpp
else if (Type_layer == 'a') {
    // element-wise add
}
```

每个 PE 对自己负责的 Add 输出神经元执行：

```text
out[i] = input0[i] + input1[i]
```

如果 Add 后带 ReLU：

```text
out[i] = max(0, input0[i] + input1[i])
```

然后写入：

```cpp
res[pic][local_id] = out;
```

计算完成后，Add 层像普通层一样进入发送流程，把结果发给后继层。

## 10. 量化计算需要注意的问题

残差加法对量化比较敏感。

最简单的实现方式是：导出 ResNet 模型时强制两个分支使用相同的量化 scale 和 zero point。这样 Add 层可以直接整数相加，复杂度最低。

更严格的实现方式是：Add 层模型文件中保存两个输入分支和输出的量化参数：

```text
src0_scale src0_zero_point
src1_scale src1_zero_point
out_scale out_zero_point
```

计算公式为：

```text
real0 = (q0 - zp0) * scale0
real1 = (q1 - zp1) * scale1
qout = round((real0 + real1) / out_scale + out_zp)
```

最后再做 int16 饱和截断。

为了降低第一版实现难度，建议先采用“导出阶段统一残差两端 scale”的方案，等 ResNet-8 跑通后再考虑更精确的重量化。

## 11. Projection Shortcut 的处理

ResNet 中有两类 shortcut：

1. identity shortcut：输入直接连接到 Add。
2. projection shortcut：输入先经过 1x1 卷积，再连接到 Add。

identity shortcut 可以直接建残差边：

```text
Layer A -> Add
```

projection shortcut 建议复用已有卷积层：

```text
Layer A -> 1x1 Conv -> Add
```

这样 1x1 卷积的权重、偏置、计算和通信都可以沿用当前卷积层实现，不需要给 shortcut 单独写一套特殊逻辑。

## 12. 近似通信方案的兼容性

如果残差连接被实现为真实的层间通信，则现有近似通信方案理论上可以直接应用：

| 方案 | 对残差连接的影响 |
|---|---|
| 精确通信 | 残差分支完整传输 |
| zero-skip | 残差分支中的 0 值可被跳过 |
| ABDTR | 残差分支也按对应层的 drop interval 丢弃 |
| all-zero-packet | 残差分支中全 0 包可被压缩 |
| FAS/isapprox | 残差分支数据可按阈值近似 |
| SAP-RLE/SAP-RLE-V2 | 残差分支数据可按相似性压缩 |

需要注意：阈值表、level 表、drop 表都应包含 Add 层对应行。即使 Add 层没有权重，它也有输出通信，因此后续通信仍需要读取这一层的近似配置。

## 13. 缓存复用与失效

残差连接会改变通信图，因此通信缓存必须重新生成。

建议规则：

1. 坐标缓存：如果层尺寸、神经元数量、映射方式变化，应重新生成。
2. 通信缓存：只要新增 Add 层或残差边，就必须重新生成。
3. cache signature 应包含：
   - 模型文件内容或 hash。
   - `groupsize`。
   - `dimx/dimy/dimz`。
   - mapping 策略。
   - 残差连接来源表。

不能复用 AlexNet、VGG 或旧顺序模型的通信缓存来跑 ResNet。

## 14. 导出脚本修改思路

如果使用 PyTorch 导出 ResNet-8，需要导出以下文件：

1. `resnet8_model.txt`
2. `resnet8_weight_fc_wb2parser.txt`
3. `resnet8_input.txt`
4. `resnet8_label.txt`
5. `resnet8_approx.txt`
6. `resnet8_approx_level_table.txt`
7. `resnet8_sap_threshold.txt`
8. `resnet8_sap_level_table.txt`
9. `resnet8_drop.txt`

导出 BasicBlock 时，可以按如下方式展开：

```text
block_input
  -> Conv1
  -> Conv2
  -> Add(Conv2, block_input)
  -> ReLU
```

如果存在降采样：

```text
block_input
  -> Conv1
  -> Conv2
  -> Add(Conv2, ProjectionConv(block_input))
  -> ReLU
```

其中 `ProjectionConv` 可以直接导出为已有的 `Convolution` 层。

## 15. 建议实施步骤

建议分阶段实现，避免一次性改动过大。

### 阶段 1：只扩展模型解析

1. 新增 `Add` 层解析。
2. 新增 `all_layer_input_layers`。
3. 普通层默认输入来源为上一层。
4. 打印解析后的 layer graph，确认残差边正确。

这个阶段不改通信和计算，只验证模型描述能力。

### 阶段 2：实现 Add 层计算

1. 在 PE 中新增 `Type_layer == 'a'` 的计算分支。
2. 暂时只在一个极小 toy model 上验证 Add 数值。
3. 先不启用近似通信。

### 阶段 3：实现多前驱接收

1. Add 层的 `receive_Neu_ID` 包含两个来源层对应神经元。
2. `should_receive` 等于两个来源分支的数据总数。
3. 等两个分支数据都到达后再开始 Add 计算。

### 阶段 4：实现多后继发送

1. 用 `layer_consumers` 替代单纯的 `ID_layer + 1`。
2. 当前层可以向多个后继层发送。
3. 普通 CNN 的消费者表仍然只有一个后继，因此旧模型行为保持一致。

### 阶段 5：重建通信缓存

1. 修改通信预计算逻辑。
2. cache signature 中加入 layer graph 信息。
3. 对旧 AlexNet/VGG/LeNet5 做精确通信回归测试。
4. 对 toy residual model 做通信正确性测试。

### 阶段 6：验证 ResNet-8

1. 先跑单图精确通信。
2. 对比 PyTorch 导出结果和 cnn-noxim 推理结果。
3. 再启用 ABDTR、FAS、SAP-RLE-V2 等近似方案。
4. 统计 flit、通信时间、网络能耗、总能耗、精度变化。

## 16. 主要风险点

1. 当前代码里可能有多处隐含 `ID_layer + 1` 假设，需要系统性排查。
2. Add 层有两个输入来源，`receive_data` 的索引必须稳定，不能把两个分支同位置数据混在一起。
3. 残差分支会增加通信边，通信缓存生成时间可能明显变长。
4. 量化 scale 如果处理不好，数值误差会比普通卷积层更明显。
5. Add 层没有权重，但有计算能耗和输出通信，统计时不能漏掉。
6. 如果 shortcut 分支跨越多个层，发送端必须能把同一层输出同时发送给主路径后继和 Add 层。

## 17. 推荐修改文件范围

预计需要修改以下文件：

| 文件 | 修改内容 |
|---|---|
| `src/NNModel.h` | 新增 layer graph、Add 层元数据结构 |
| `src/NNModel.cpp` | 解析 Add 层、建立输入来源表和消费者表、修改通信缓存预计算 |
| `src/NoximProcessingElement.cpp` | Add 层接收初始化、Add 层计算、多后继发送支持 |
| ResNet 导出脚本 | 导出 ResNet-8 模型、权重、输入、阈值表、level 表、drop 表 |

如果第一版不新增命令行参数，则 `NoximCmdLineParser.cpp` 和 `NoximMain.h` 可以尽量不动。

## 18. 最小验证网络

建议先构造一个极小残差网络，不直接上 ResNet-8：

```text
Input
Conv1
Conv2
Add(Conv2, Conv1)
Dense
```

验证内容：

1. `Conv1` 输出是否同时发送给 `Conv2` 和 `Add`。
2. `Conv2` 输出是否发送给 `Add`。
3. `Add` 是否等待两个来源都收齐后再计算。
4. `Add` 输出是否能继续发送给 `Dense`。
5. 精确通信下输出是否和 Python/PyTorch 参考结果一致。
6. 近似通信下 flit 统计是否包含残差分支通信。

## 19. 结论

最推荐的实现路线是：新增显式 `Add` 层，并把当前线性层间通信扩展为通用 layer graph 通信。

这个方案改动量比“把残差加法偷偷融合进某个卷积层”更大，但它更适合 cnn-noxim 的 NoC 仿真目标，因为残差分支的数据会真实经过网络，网络状态、flit 数量、通信能耗和近似通信效果才可信。

短期目标可以先支持 ResNet-8 中最常见的双输入 Add；projection shortcut 通过已有 1x1 卷积层表达。等 ResNet-8 精确模式跑通后，再逐步启用 FAS、ABDTR、SAP-RLE-V2 等近似通信方案。
