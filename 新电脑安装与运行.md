# 新电脑安装与运行说明

本文档用于在一台新的 Linux 电脑上，从 GitHub 私有仓库拉取 `cnn-noxim-v3`，安装依赖，编译 Noxim，并运行 CNN-NoC 仿真实验。

以下命令默认使用 Ubuntu/Debian 系统。如果使用其他 Linux 发行版，包管理器命令需要相应调整。

## 1. 克隆仓库

私有仓库需要先确认新电脑已经能访问 GitHub，并且 GitHub 账号有该仓库权限。

如果使用 HTTPS：

```bash
cd ~
git clone https://github.com/chunyu765-byte/cnn-noxim-v3.git
cd cnn-noxim-v3
```

如果使用 SSH：

```bash
cd ~
git clone git@github.com:chunyu765-byte/cnn-noxim-v3.git
cd cnn-noxim-v3
```

如果 SSH 的 22 端口不通，可以优先使用 HTTPS，或把 SSH 改成 443 端口。

## 2. 安装系统依赖

编译 C++ 仿真器需要 `g++`、`make` 和 SystemC。新 Ubuntu 上优先使用发行版自带的 `libsystemc-dev`，不再要求手动安装旧的 SystemC 2.3.1。Python 导出脚本建议使用 Python 3.10 左右的独立环境。

```bash
sudo apt update
sudo apt install -y build-essential git make wget tar pkg-config libsystemc-dev python3 python3-venv python3-pip
```

## 3. 安装 SystemC

当前 `bin/Makefile.defs` 会优先通过 `pkg-config systemc` 自动查找系统安装的 SystemC。新 Ubuntu 推荐直接安装：

```bash
sudo apt install -y pkg-config libsystemc-dev
```

检查方式：

```bash
pkg-config --modversion systemc
pkg-config --cflags --libs systemc
```

如果这些命令能输出版本、头文件路径和库参数，就不需要再安装旧版 SystemC。

如果系统源里没有 `libsystemc-dev`，或者你需要复现实验时的老环境，`bin/Makefile.defs` 仍然保留了旧路径兜底：

```text
/usr/local/systemc-2.3.1
```

如果新电脑上已经有这个目录，并且包含：

```text
/usr/local/systemc-2.3.1/include/systemc.h
/usr/local/systemc-2.3.1/lib-linux64/libsystemc.a
```

也可以直接编译。

手动安装 SystemC 2.3.1 的示例：

```bash
cd ~/Downloads
wget https://www.accellera.org/images/downloads/standards/systemc/systemc-2.3.1.tgz
tar -xzf systemc-2.3.1.tgz
cd systemc-2.3.1
mkdir objdir
cd objdir
../configure --prefix=/usr/local/systemc-2.3.1
make -j"$(nproc)"
sudo make install
```

安装完成后检查：

```bash
ls /usr/local/systemc-2.3.1/include/systemc.h
ls /usr/local/systemc-2.3.1/lib-linux64/libsystemc.a
```

较新的 Linux/C++ 环境中，手动安装 SystemC 2.3.1 可能会因为 C 标准库里的 `gets` 已被移除而编译失败。典型报错类似：

```text
error: 'gets' has not been declared in 'std'
```

如果出现这个报错，需要修改 SystemC 安装目录下的头文件：

```bash
sudo nano /usr/local/systemc-2.3.1/include/systemc.h
```

找到这一行：

```cpp
using std::gets;
```

把它注释掉：

```cpp
// using std::gets;
```

当前项目并不使用 `gets`，注释这一行通常不会影响仿真器编译和运行。本机当前 SystemC 环境已经是注释后的形式。

如果你手动安装的 SystemC 在其他路径，需要修改：

```bash
nano bin/Makefile.defs
```

把这一行：

```makefile
SYSTEMC ?= /usr/local/systemc-2.3.1
```

改成你的实际 SystemC 安装路径。

## 4. 编译 Noxim

进入 `bin` 目录后执行 `make`：

```bash
cd ~/cnn-noxim-v3/bin
make clean
make -j"$(nproc)"
```

编译成功后应生成：

```text
bin/build/noxim
```

检查方式：

```bash
ls -l build/noxim
```

## 5. 准备输出目录

仿真运行时会写入 `results/` 和 `run_log/`。新电脑第一次运行前建议创建目录：

```bash
cd ~/cnn-noxim-v3/bin
mkdir -p run_log
mkdir -p results/POWER results/STEADY results/STLD results/TEMP results/buffer
```

## 6. 运行 ResNet8-CIFAR10 示例

推荐先运行一次较短的 smoke test，确认程序能启动和读取模型文件：

```bash
cd ~/cnn-noxim-v3/bin
./build/noxim \
  -dimx 8 -dimy 8 -dimz 1 \
  -NNmodel resnet8_cifar10/resnet8_model.txt \
  -NNweight resnet8_cifar10/resnet8_weight_fc_wb2parser.txt \
  -NNweight_scale resnet8_cifar10/resnet8_weight_scale.txt \
  -NNinput resnet8_cifar10/resnet8_input.txt \
  -NNapprox resnet8_cifar10/resnet8_approx.txt \
  -NNapprox_Level_Table resnet8_cifar10/resnet8_approx_level_table.txt \
  -NNlabel resnet8_cifar10/resnet8_label.txt \
  -mapping dir_x \
  -groupsize 4096 \
  -pe_log 0 \
  -isapprox 0 \
  -allzeropacket 0 \
  -zero_skip 0 \
  -acdc_abdtr 0 \
  -is_sap_rle 0 \
  -is_sap_rle_v2 0 \
  -sim 20000 \
  -stop_on_infer_done 1
```

如果 smoke test 正常，再运行完整脚本：

```bash
cd ~/cnn-noxim-v3/bin
bash run_resnet8_cifar10_all_modes_cpu1-2.sh
```

该脚本会依次运行：

```text
exact
abdtr
fas
sap-rle
sap-rle-v2
```

日志输出在：

```text
bin/run_log/
```

## 7. 运行 AlexNet-CIFAR10 示例

```bash
cd ~/cnn-noxim-v3/bin
LOG_DATE="$(date '+%y_%m_%d')"
mkdir -p run_log

./build/noxim \
  -dimx 8 -dimy 8 -dimz 1 \
  -NNmodel alexnet_cifar10/alexnet_model.txt \
  -NNweight alexnet_cifar10/alexnet_weight_fc_wb2parser.txt \
  -NNweight_scale alexnet_cifar10/alexnet_weight_scale.txt \
  -NNinput alexnet_cifar10/alexnet_input.txt \
  -NNapprox alexnet_cifar10/alexnet_approx.txt \
  -NNapprox_Level_Table alexnet_cifar10/alexnet_approx_level_table.txt \
  -NNlabel alexnet_cifar10/alexnet_label.txt \
  -mapping dir_x \
  -groupsize 1024 \
  -pe_log 0 \
  -isapprox 0 \
  -allzeropacket 0 \
  -zero_skip 0 \
  -acdc_abdtr 0 \
  -is_sap_rle 0 \
  -is_sap_rle_v2 0 \
  -sim 4000000 \
  -stop_on_infer_done 1 \
  > "run_log/run_log_${LOG_DATE}_alexnet_cifar10_exact_8x8x1_gs1024_sim4e6.txt" 2>&1
```

更多命令可以参考：

```text
常用命令.md
```

## 8. Python 环境

如果只运行已经导出的 `bin/resnet8_cifar10/*.txt` 或 `bin/alexnet_cifar10/*.txt`，通常不需要 Python 环境。

如果需要重新导出模型、输入、权重或近似配置文件，需要安装 Python 依赖。

推荐使用虚拟环境：

```bash
cd ~/cnn-noxim-v3
python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
pip install -r requirements.txt
```

原始工作环境记录为：

```text
python==3.10.20
torch==2.11.0+cu130
numpy==2.2.6
Pillow==12.2.0
```

如果新电脑没有 CUDA，使用 CPU 版 PyTorch 也可以运行导出脚本，只是速度可能更慢。

## 9. 重新生成 Noxim 输入文件

仓库里保留了已经导出的 Noxim 文本输入文件，通常可以直接运行仿真。

如果需要重新导出 ResNet8：

```bash
cd ~/cnn-noxim-v3
source .venv/bin/activate
python Resnet8_CIFAR10/export_resnet8_cifar10_to_noxim.py
```

如果需要重新导出 AlexNet：

```bash
cd ~/cnn-noxim-v3
source .venv/bin/activate
python AlexNet_CIFAR10/export_alexnet_cifar10_to_noxim.py
```

注意：原始 CIFAR10 数据集目录 `Resnet8_CIFAR10/data/` 没有上传到 GitHub。如果某个 Python 脚本需要原始 CIFAR10 数据，需要在新电脑上重新下载或手动放入该目录。

## 10. 常见问题

### GitHub 私有仓库无法 clone

如果 HTTPS 需要密码，不要输入 GitHub 登录密码，而是使用 Personal Access Token。

如果 SSH 报错：

```text
ssh: connect to host github.com port 22: Connection timed out
```

说明当前网络可能屏蔽了 SSH 22 端口。可以改用 HTTPS，或者配置 SSH 走 443 端口。

### 编译时报 systemc.h 找不到

新 Ubuntu 先检查系统包是否安装：

```bash
pkg-config --modversion systemc
```

如果提示找不到 `systemc`，安装：

```bash
sudo apt install -y pkg-config libsystemc-dev
```

如果不用系统包，而是手动安装 SystemC，再检查旧路径：

```bash
ls /usr/local/systemc-2.3.1/include/systemc.h
```

如果路径不同，修改：

```text
bin/Makefile.defs
```

里的：

```makefile
SYSTEMC ?= /usr/local/systemc-2.3.1
```

### 编译时报 libsystemc.a 找不到

如果使用 `libsystemc-dev` 系统包，当前 Makefile 不再依赖这个静态库路径。先确认：

```bash
pkg-config --libs systemc
```

如果不用系统包，再检查：

检查：

```bash
ls /usr/local/systemc-2.3.1/lib-linux64/libsystemc.a
```

如果你的 SystemC 库目录不是 `lib-linux64`，需要同步修改 `bin/Makefile.defs` 中的库路径规则。

### 编译时报 gets 没有声明

如果报错类似：

```text
error: 'gets' has not been declared in 'std'
```

这是 SystemC 2.3.1 与较新的 C++ 标准库不兼容导致的。修改：

```bash
sudo nano /usr/local/systemc-2.3.1/include/systemc.h
```

把：

```cpp
using std::gets;
```

改成：

```cpp
// using std::gets;
```

如果 SystemC 安装路径不是 `/usr/local/systemc-2.3.1`，请改为你的实际路径。

### 运行时报 results 文件无法打开

先创建输出目录：

```bash
cd ~/cnn-noxim-v3/bin
mkdir -p results/POWER results/STEADY results/STLD results/TEMP results/buffer
```

### 运行脚本提示 taskset 或 CPU 核心不可用

`run_resnet8_cifar10_all_modes_cpu1-2.sh` 默认使用 CPU 核心 `1,2`：

```bash
CPU_CORES="1,2"
```

如果新电脑核心数量不足，修改为可用核心，例如：

```bash
CPU_CORES="0,1"
```

### 第一次完整仿真很慢

完整命令使用：

```text
-sim 4000000
```

第一次测试可以先把 `-sim` 改小，例如：

```text
-sim 20000
```

确认程序能跑通后再运行完整实验。

## 11. 推荐的新电脑检查顺序

```bash
cd ~/cnn-noxim-v3
git status

cd bin
make clean
make -j"$(nproc)"

mkdir -p run_log
mkdir -p results/POWER results/STEADY results/STLD results/TEMP results/buffer

./build/noxim -dimx 8 -dimy 8 -dimz 1 \
  -NNmodel resnet8_cifar10/resnet8_model.txt \
  -NNweight resnet8_cifar10/resnet8_weight_fc_wb2parser.txt \
  -NNweight_scale resnet8_cifar10/resnet8_weight_scale.txt \
  -NNinput resnet8_cifar10/resnet8_input.txt \
  -NNapprox resnet8_cifar10/resnet8_approx.txt \
  -NNapprox_Level_Table resnet8_cifar10/resnet8_approx_level_table.txt \
  -NNlabel resnet8_cifar10/resnet8_label.txt \
  -mapping dir_x \
  -groupsize 4096 \
  -sim 20000 \
  -stop_on_infer_done 1
```

如果以上命令能完成，说明新电脑的基本安装和运行环境已经配置成功。
