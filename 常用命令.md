# 常用命令

以下命令默认在 `cnn-noxim-v2/bin` 目录下执行。

运行前先生成当天日期：

```bash
LOG_DATE="$(date '+%y_%m_%d')"
```

## AlexNet-CIFAR10 精确通信

```bash
./build/noxim \
  -dimx 8 -dimy 8 -dimz 1 \
  -NNmodel alexnet_cifar10/alexnet_model.txt \
  -NNweight alexnet_cifar10/alexnet_weight_fc_wb2parser.txt \
  -NNweight_scale alexnet_cifar10/alexnet_weight_scale.txt \
  -NNinput alexnet_cifar10/alexnet_input.txt \
  -NNapprox alexnet_cifar10/alexnet_approx.txt \
  -NNapprox_Level_Table alexnet_cifar10/alexnet_approx_level_table.txt \
  -NNlabel alexnet_cifar10/alexnet_label.txt \
  -mapping dir_x \
  -groupsize 1024 \
  -pe_log 0 \
  -isapprox 0 \
  -allzeropacket 0 \
  -zero_skip 0 \
  -acdc_abdtr 0 \
  -is_sap_rle 0 \
  -is_sap_rle_v2 0 \
  -sim 4000000 \
  -stop_on_infer_done 1 \
  > "run_log/run_log_${LOG_DATE}_alexnet_cifar10_exact_8x8x1_gs1024_sim4e6.txt" 2>&1
```

## AlexNet-CIFAR10 FAS

```bash
./build/noxim \
  -dimx 8 -dimy 8 -dimz 1 \
  -NNmodel alexnet_cifar10/alexnet_model.txt \
  -NNweight alexnet_cifar10/alexnet_weight_fc_wb2parser.txt \
  -NNweight_scale alexnet_cifar10/alexnet_weight_scale.txt \
  -NNinput alexnet_cifar10/alexnet_input.txt \
  -NNapprox alexnet_cifar10/alexnet_approx.txt \
  -NNapprox_Level_Table alexnet_cifar10/alexnet_approx_level_table.txt \
  -NNlabel alexnet_cifar10/alexnet_label.txt \
  -mapping dir_x \
  -groupsize 1024 \
  -pe_log 0 \
  -isapprox 1 \
  -allzeropacket 0 \
  -zero_skip 0 \
  -acdc_abdtr 0 \
  -is_sap_rle 0 \
  -is_sap_rle_v2 0 \
  -sim 4000000 \
  -stop_on_infer_done 1 \
  > "run_log/run_log_${LOG_DATE}_alexnet_cifar10_fas_8x8x1_gs1024_sim4e6.txt" 2>&1
```

## AlexNet-CIFAR10 Tanh 精确通信

```bash
./build/noxim \
  -dimx 8 -dimy 8 -dimz 1 \
  -NNmodel alexnet_cifar10_tanh/alexnet_model.txt \
  -NNweight alexnet_cifar10_tanh/alexnet_weight_fc_wb2parser.txt \
  -NNweight_scale alexnet_cifar10_tanh/alexnet_weight_scale.txt \
  -NNinput alexnet_cifar10_tanh/alexnet_input.txt \
  -NNapprox alexnet_cifar10_tanh/alexnet_approx.txt \
  -NNapprox_Level_Table alexnet_cifar10_tanh/alexnet_approx_level_table.txt \
  -NNlabel alexnet_cifar10_tanh/alexnet_label.txt \
  -mapping dir_x \
  -groupsize 1024 \
  -pe_log 0 \
  -isapprox 0 \
  -allzeropacket 0 \
  -zero_skip 0 \
  -acdc_abdtr 0 \
  -is_sap_rle 0 \
  -is_sap_rle_v2 0 \
  -sim 4000000 \
  -stop_on_infer_done 1 \
  > "run_log/run_log_${LOG_DATE}_alexnet_cifar10_tanh_exact_8x8x1_gs1024_sim4e6.txt" 2>&1
```

## 命名规则

新的仿真日志统一使用：

```text
run_log/run_log_${LOG_DATE}_网络名_方案_配置.txt
```

其中 `LOG_DATE` 由 `date '+%y_%m_%d'` 在运行时生成。例如 2026 年 8 月 31 日运行时，日志名前缀为：

```text
run_log/run_log_26_08_31_...
```
