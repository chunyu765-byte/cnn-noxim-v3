# 修改日志

## 2026-07-09 SAP-RLE-V2 全包相似锚点压缩

- 位置：`src/NoximProcessingElement.cpp`
- 修改前备份：
  - `backups/2026-07-09_saprle_v2_before_change/NoximProcessingElement.cpp.bak`
  - `backups/2026-07-09_saprle_v2_before_change/修改日志.md.bak`
- 发送端改动：
  - 仅在 `NoximGlobalParams::is_sap_rle_v2` 分支内调整逻辑，不改动精确、FAS、0 跳过、ABDTR、allzeropacket、SAP-RLE v1、drop/trunc 等其它方案。
  - 当包内所有 BODY flit 与第一个 BODY 锚定值的绝对差不超过 `sap_rle_delta` 时，将锚定值写入 HEAD flit 的 `data` 字段，并记录所有 BODY flit 的 `approx_pos`、`approx_level` 与 `approx_src_id`，随后删除全部 BODY flit，只传输 HEAD/TAIL。
  - 若不满足整包相似条件，继续复用原 SAP-RLE 的部分压缩逻辑：保留锚点 BODY，仅删除与当前锚点足够相似的后续 BODY。
- 接收端改动：
  - SAP-RLE/SAP-RLE-V2 恢复仍优先使用左侧最近 BODY，其次右侧最近 BODY。
  - 针对 SAP-RLE-V2 的 HEAD/TAIL-only 包，若恢复时包内没有任何 BODY，则使用 HEAD flit 中保存的锚定值恢复被删除的 BODY flit。

## 2026-04-29 重建版（重点：仿真加速与 cache 机制）

> 说明：本文件为根目录 `修改日志.md` 的重建版。只记录当前仓库中可核对到的实现与结论。

---

## 1. 仿真加速相关改动

### 1.1 卷积/池化坐标缓存（已启用）

- 位置：`src/NNModel.cpp`
- 核心函数：
  - `saveCoordCache(int wr, string model_filename)`
  - `loadCoordCache(int wr, string model_filename)`
- 缓存文件：`<model_path>.coord_cache`

#### 工作方式

1. 首次运行某个模型：
   - 计算 `all_conv_coord` 与 `all_pool_coord`
   - 计算结束后落盘成 `.coord_cache`
2. 后续运行同一路径模型：
   - 优先读取 `.coord_cache`
   - 命中后跳过大规模坐标重算

#### 日志特征

- 首次：`Saved coordinate cache to ...`
- 命中：`Loaded convolution/pooling coordinates from cache for ...`

#### 加速收益

- 对深层 CNN（如 VGG）在启动阶段能显著减少“模型加载 + 坐标构建”耗时。
- 对重复实验（同一模型文件路径反复跑）收益最明显。

---

### 1.2 Global Traffic Table 预计算（已启用）

- 位置：`src/NNModel.cpp`
- 关键日志：
  - `Starting Global Traffic Table Pre-computation...`
  - `Global Traffic Table Pre-computation Completed.`
- 预计算数据结构：
  - `PE_send_list`
  - `PE_send_req_list`
  - `PE_send_conv_list`
  - `PE_send_pool_list`
  - `PE_receive_conv_list`

#### 工作方式

在 `NNModel::load()` 阶段预生成 PE 间发送/接收关系，运行时 `rxProcess` 直接查表使用，避免频繁在 `Group_table` 上做重复扫描和匹配。当前版本支持落盘缓存并在命中时跳过重算。

#### 加速收益

- 减少运行期每个 PE 的关系推导开销。
- 对网络层数多、PE 数量大时更稳定。

---

### 1.3 坐标构建 OpenMP 并行化（已启用）

- 位置：`src/NNModel.cpp`
- 方式：在卷积/池化坐标构建中使用 `#pragma omp parallel for collapse(2)`。

#### 加速收益

- 首次构建 `.coord_cache` 时可利用多核 CPU，缩短预处理时间。

---

### 1.4 减少日志 I/O 干扰（实验策略）

- 方式：做长仿真时尽量关闭/减少 PE 细粒度打印，日志只保留关键统计。
- 目的：避免大量文件写入拖慢仿真主循环。

---

## 2. `traffic_cache` 状态更新（2026-04-29 最新）

### 2.1 当前源码状态（已启用，单一格式）

当前仓库源码中，`traffic_cache` 已接入，且统一为单一格式：

- 新增接口：`NNModel::saveTrafficCache()` / `NNModel::loadTrafficCache()`
- 接入位置：`NNModel::load()` 的 Global Traffic Table 预计算阶段
- 缓存文件：`<model_path>.traffic_cache`
- 缓存格式：`TRFCCHE1`（`TRFC` + `CHE1`）

实现行为：

1. 首次运行（无 cache）：
   - 正常执行 `Starting Global Traffic Table Pre-computation...`
   - 结束后写入 `.traffic_cache`
2. 后续同配置运行（命中 cache）：
   - 直接加载 `.traffic_cache`
   - 跳过大部分 GTT 重算

关键日志：

- `Saved Global Traffic Table cache (TRFCCHE1) to ...`
- `Loaded traffic cache format TRFCCHE1 from ...`
- `Loaded Global Traffic Table from cache for ...`

### 2.2 缓存一致性保护

`traffic_cache` 读取时会校验以下字段（当前版本）：

- `wr`（按历史格式保存为 `wr+1`）
- `mesh_dim_x/y/z`
- `total_pes`（当前实现是 `mesh_dim_x * mesh_dim_y`）
- `group_neu_num`（groupsize）

说明：

1. 当前不再做多格式兼容（仅识别 `TRFCCHE1`）。
2. 几何参数不匹配则回退重算。
3. 若几何参数匹配但签名不一致，当前实现会“按几何继续使用缓存”，并自动将缓存头中的签名刷新为当前算法值，避免后续重复 mismatch 提示。

### 2.3 本次验证（vgg16_cifar10）

- 验证命令：`8x8x1, groupsize=6144, mapping=dir_x, -pe_log 0`
- 第一次：`run_log/run_log_26_04_29_vgg16_sig_refresh_pass1.txt`
  - 关键日志：`TRFCCHE1 signature refreshed in cache...`
  - 随后命中：`Loaded ... from cache`
- 第二次：`run_log/run_log_26_04_29_vgg16_sig_refresh_pass2.txt`
  - 关键日志：不再出现 signature mismatch
  - 直接命中：`Loaded ... from cache`

---

## 3. 加速效果实测（lenet5, exact, 8x8x1, groupsize=1024, sim=40000）

### 3.1 历史数据（较早记录）

- Run1: `158.39s`
- Run2: `156.09s`
- 第二次相对第一次仅约 `1.5%` 提升

### 3.2 历史数据（较早记录）

- Run1: `157.44s`
- Run2: `20.51s`
- 第二次相对第一次约 `7.68x` 加速
- 第二次相对“优化前 Run2”减少 `135.58s`（约 `86.86%`）

### 3.3 当前实测（2026-04-29）

- 测试命令：`-warmup 0 -sim 40000 -pe_log 0`
- Pass1 日志：`run_log/run_log_26_04_29_lenet5_exact_speed_pass1.txt`
- Pass2 日志：`run_log/run_log_26_04_29_lenet5_exact_speed_pass2.txt`
- 实测 wall-time：
  - Pass1：`69.59s`
  - Pass2：`71.81s`
- 两次均命中 `traffic_cache`（均打印 `Loaded Global Traffic Table from cache ...`）。

当前结论：

- 当前版本下，`lenet5` 这组参数未复现“约 20s”级运行时间；虽已命中 cache，但总 wall-time 仍约 70s。

---

## 4. PE 日志开关优化（命令行可控）

### 4.1 目标

- 将 `PE_R_x.txt` / `PE_T_x.txt` 日志改为可开关
- 默认保持原行为（开启）

### 4.2 新增参数

- `-pe_log 1`：开启（默认）
- `-pe_log 0`：关闭

接入点：

- 全局参数：`NoximGlobalParams::pe_log_enable = 1`
- 命令行解析：`NoximCmdLineParser.cpp` 新增 `-pe_log`
- 参数校验：仅允许 `0/1`
- `NoximProcessingElement.cpp` 的 RX/TX 日志写入点统一受该开关控制

### 4.3 验证结果

- `-pe_log 0`：`PE_log` 文件数 `COUNT_OFF=0`
- `-pe_log 1`：`PE_log` 文件数 `COUNT_ON=20`
- 两组仿真精度一致：`accuracy: 1`

---

## 5. 复现建议

- 若要测“第二次加速”，请保持模型路径、NoC 维度、groupsize、mapping 不变。
- 若改了模型结构或映射配置，建议删除对应 `.coord_cache/.traffic_cache` 后重跑一次冷启动。

---

## 2026-07-10 SAP-RLE 文件化逐层阈值

### 1. 备份

- 备份目录：`backups/2026-07-10_sap_file_threshold_before_change/`
- 已备份：
  - `src/NoximMain.h`
  - `src/NoximMain.cpp`
  - `src/NoximCmdLineParser.cpp`
  - `src/NNModel.h`
  - `src/NNModel.cpp`
  - `src/NoximProcessingElement.cpp`
  - `修改日志.md`
  - `bin/lenet5/approx.txt`
  - `bin/lenet5/approx_level_table.txt`

### 2. 目标

- 模仿 FAS/isapprox 的“阈值表 + 阈值配置表”机制，为 SAP-RLE/SAP-RLE-V2 增加逐层阈值选择。
- SAP-RLE/SAP-RLE-V2 不再依赖命令行直接指定单一 `sap_rle_delta`；运行时按当前层从文件生成的逐层阈值数组中取值。
- 旧 `sap_rle_delta` 字段保留为兜底值，避免意外未加载数组时崩溃。

### 3. 新增参数

- `-sap_threshold_file FILENAME`：SAP-RLE 逐层候选阈值表。
- `-sap_level_table_file FILENAME`：SAP-RLE 逐层阈值等级配置表。
- 仅当 `-is_sap_rle 1` 或 `-is_sap_rle_v2 1` 时读取和检查上述两个文件。
- 阈值等级选择复用 `-config_sel N`。

### 4. 文件格式

阈值表每层一行，顺序必须与模型非输入层一致：

```text
Convolution 0 128 256 512
Pooling 0 128 256 512
Dense 0 128 256 512
```

等级配置表每层一行，6 列对应 `config_sel=0..5`：

```text
Convolution 0 1 2 3 3 3
Pooling 0 1 2 3 3 3
Dense 0 1 2 3 3 3
```

等级含义：

- `0 ->` 该层阈值表第 1 个值
- `1 ->` 该层阈值表第 2 个值
- `2 ->` 该层阈值表第 3 个值
- `3 ->` 该层阈值表第 4 个值

### 5. LeNet5 测试文件

- `bin/lenet5/lenet5_sap_threshold.txt`
- `bin/lenet5/lenet5_sap_level_table.txt`

### 6. AlexNet-CIFAR10 测试文件

- `bin/alexnet_cifar10/alexnet_sap_threshold.txt`
- `bin/alexnet_cifar10/alexnet_sap_level_table.txt`
- 默认 `config_sel=0` 选择逐层混合阈值：`256, 0, 256, 0, 128, 128, 256, 0, 256, 128, 0`
- `config_sel=2` 对应逐层全 `256`，用于和默认混合阈值做 flit 数对照。

### 7. AlexNet-CIFAR10 SAP-RLE 验证

- 默认混合阈值日志：`bin/run_log/run_log_26_07_10_alexnet_cifar10_saprle_filethreshold_cfg0_mixed_sim220k_nopelog.txt`
- 默认混合阈值结果：`Total received flits = 139518`，`accuracy = 1`
- 参考全 `0` 阈值日志：`bin/run_log/ALEXNET-CIFAR10/run_log_alexnet_cifar10_sap_rle_d0_8x8x1_gs1024_sim4e6.txt`，`Total received flits = 146057`
- 参考全 `256` 阈值日志：`bin/run_log/ALEXNET-CIFAR10/run_log_alexnet_cifar10_sap_rle_d256_8x8x1_gs1024_sim4e6.txt`，`Total received flits = 135278`
- 结论：默认混合阈值的 flit 数介于全 `0` 和全 `256` 之间。

---

## 2026-07-13 ABDTR 文件化逐层丢弃间隔

### 1. 备份

- 备份目录：`backups/2026-07-13_abdtr_drop_file_before_change/`
- 已备份：
  - `src/NoximMain.h`
  - `src/NoximMain.cpp`
  - `src/NoximCmdLineParser.cpp`
  - `src/NNModel.h`
  - `src/NNModel.cpp`
  - `src/NoximProcessingElement.cpp`
  - `修改日志.md`

### 2. 改动

- 新增命令行参数：`-abdtr_drop_file FILENAME`。
- 仅当 `-acdc_abdtr 1` 时读取和校验 ABDTR drop 文件；未启用 ABDTR 时不读取、不检查。
- drop 文件支持两种格式：
  - 只有 1 个整数：所有非输入层共用同一个丢弃间隔。
  - 非输入层数量个整数：每一层使用一个独立丢弃间隔。
- 发送端继续按 `ID_layer-1` 读取当前层丢弃间隔，并增加边界保护。

### 3. LeNet5 验证文件

- `bin/lenet5/lenet5_drop_i1.txt`：所有非输入层间隔为 `1`
- `bin/lenet5/lenet5_drop_i2.txt`：所有非输入层间隔为 `2`
- `bin/lenet5/lenet5_drop_global_i1.txt`：单值全局间隔 `1`
- `bin/lenet5/lenet5_drop_global_i2.txt`：单值全局间隔 `2`
- `bin/lenet5/lenet5_drop_mixed_i1_i2.txt`：逐层混合 `1/2`

### 4. LeNet5 验证结果

- 全局 `1` 间隔日志：`bin/run_log/run_log_26_07_13_lenet5_abdtr_dropfile_global_i1_nopelog.txt`
  - `Total received flits = 7726`
  - `accuracy = 1`
- 全局 `2` 间隔日志：`bin/run_log/run_log_26_07_13_lenet5_abdtr_dropfile_global_i2_nopelog.txt`
  - `Total received flits = 9272`
  - `accuracy = 1`
- 混合 `1/2` 间隔日志：`bin/run_log/run_log_26_07_13_lenet5_abdtr_dropfile_mixed_i1_i2_nopelog.txt`
  - `Total received flits = 8199`
  - `accuracy = 1`
- 合理性结论：全 `1` 丢弃更频繁，flit 数最少；全 `2` 丢弃较少，flit 数最多；混合 `1/2` 结果位于两者之间。
- 非 ABDTR 模式不检查 drop 文件验证：`bin/run_log/run_log_26_07_13_lenet5_exact_missing_abdtr_dropfile_no_check.txt`

---

## 2026-07-13 卷积层激活函数补全

### 1. 备份

- 备份目录：`backups/2026-07-13_conv_activation_before_change/`
- 已备份：
  - `src/NNModel.h`
  - `src/NNModel.cpp`
  - `src/NoximProcessingElement.cpp`
  - `修改日志.md`

### 2. 改动

- 新增 `NONE_ACT` 激活标记，用于表示模型文件中的 `none` 或未识别激活函数。
- `Dense` 和 `Convolution` 解析时均支持 `none`，并对未知激活函数回退到 `NONE_ACT`。
- `NoximProcessingElement.cpp` 新增统一激活函数处理：
  - `relu`
  - `tanh`
  - `sigmoid`
  - `softmax` 的逐元素 `exp` 阶段
  - `none`
- 将卷积层原本只处理 `relu` 的输出赋值路径改为统一调用上述激活函数。

### 3. 验证

- 编译：`make -C bin` 通过。
- 原始 LeNet5 ReLU 模型验证日志：`bin/run_log/run_log_26_07_13_lenet5_exact_relu_after_conv_activation_patch.txt`
  - `accuracy = 1`
  - `Total received flits = 12354`
- 临时第一层卷积 `none` 模型短验证日志：`bin/run_log/run_log_26_07_13_lenet5_conv_none_smoke_after_activation_patch.txt`
  - 成功解析 `Convolution ... none`
  - 成功完成短仿真并输出统计。

---

## 2026-07-13 FAS/isapprox 全近似包占位 BODY flit

### 1. 备份

- 备份目录：`backups/2026-07-13_fas_placeholder_before_change/`
- 已备份：
  - `src/NoximMain.h`
  - `src/NoximProcessingElement.cpp`
  - `修改日志.md`

### 2. 改动

- 在 `NoximFlit` 中新增 `is_fas_placeholder` 属性。
- placeholder 仍使用 `FLIT_TYPE_BODY`，因此会真实进入 NoC，参与 router、buffer、link、delay、energy 和 flit 数统计。
- 仅当 `-isapprox 1` 且一个包内所有 `BODY` 都被近似删除时，额外插入 1 个 placeholder BODY flit。
- 接收端最终写入 `receive_data` 时跳过 `is_fas_placeholder == true` 的 flit，避免它污染神经元数据恢复。
- SAP-RLE、SAP-RLE-V2、ABDTR、allzeropacket、zero-skip、drop/trunc 和 exact 不使用该 placeholder 逻辑。

### 3. LeNet5 验证

- 修改前日志：`bin/run_log/run_log_26_07_13_lenet5_isapprox_before_fas_placeholder_nopelog.txt`
  - `Total received flits = 8028`
  - `Global average delay = 830.31`
  - `Total energy = 6.82363e-07`
  - `accuracy = 1`
- 修改后日志：`bin/run_log/run_log_26_07_13_lenet5_isapprox_after_fas_placeholder_nopelog.txt`
  - `Total received flits = 8450`
  - `Global average delay = 910.781`
  - `Total energy = 8.78537e-07`
  - `accuracy = 1`
- 结论：placeholder 作为真实 BODY flit 进入 NoC 后，`Total received flits` 增加 `422`，网络延迟和能耗也随之增加，符合预期。

---

## 2026-07-13 AlexNet SAP-RLE 差分阈值表生成

### 1. 备份

- 备份目录：`backups/2026-07-13_alexnet_sap_diff_threshold_before_gen/`
- 已备份：
  - `bin/alexnet_cifar10/alexnet_sap_threshold.txt`

### 2. 新增脚本

- 新增 `AlenNet_CIFAR10/stat_alexnet_sap_diff_quantiles.py`。
- 默认读取：
  - `bin/alexnet_cifar10/alexnet_model.txt`
  - `bin/alexnet_cifar10/alexnet_input.txt`
  - `bin/alexnet_cifar10/alexnet_weight_fc_wb2parser.txt`
- 按 cnn-noxim 导出的模型、输入、权重执行 16bit 量化前向计算。
- 对每一层量化输出按展平顺序统计相邻激活值的绝对差分。
- 默认只统计同号相邻数据的差分，以匹配 SAP-RLE 当前 `same_sign && abs_diff <= delta` 的压缩条件。
- 去除 0 差分后排序，使用 nearest-rank 位置取 20%、40%、60%、80% 四个阈值。

### 3. 生成文件

- 更新 `bin/alexnet_cifar10/alexnet_sap_threshold.txt`。
- 生成明细 CSV：`AlenNet_CIFAR10/analysis_csv/alexnet_sap_diff_quantiles.csv`
- 生成摘要文档：`AlenNet_CIFAR10/analysis_csv/alexnet_sap_diff_quantiles.md`

### 4. 生成结果

```text
Convolution 167 434 1050 2617
Pooling 235 646 1597 3852
Convolution 547 1381 2617 5051
Pooling 823 1949 3570 6762
Convolution 772 1720 3073 5093
Convolution 486 1124 1937 3275
Convolution 717 1708 2943 4850
Pooling 1088 2288 3935 6474
Dense 1549 3991 6735 11764
Dense 1805 4237 8031 13841
Dense 3771 5004 7352 11807
```

### 5. 验证

- `python3 AlenNet_CIFAR10/stat_alexnet_sap_diff_quantiles.py` 执行成功。
- `python3 -m py_compile AlenNet_CIFAR10/stat_alexnet_sap_diff_quantiles.py` 通过。
- `bin/alexnet_cifar10/alexnet_sap_threshold.txt` 共 11 行，与 AlexNet 模型的 11 个非输入层对应。

---

## 2026-07-13 AlexNet SAP-RLE 差分阈值 level1 仿真验证

### 1. 新增 level 表

- 新增 `bin/alexnet_cifar10/alexnet_sap_level_all_level1.txt`。
- 该文件每层 6 个配置列均为 `1`，因此默认 `config_sel=0` 时每层都选择 SAP 阈值表中的第 2 个候选阈值。
- SAP-RLE 阈值读取顺序为正序读取：
  - level 0 -> 第 1 个阈值
  - level 1 -> 第 2 个阈值
  - level 2 -> 第 3 个阈值
  - level 3 -> 第 4 个阈值

### 2. 仿真日志

- 日志：`bin/run_log/run_log_26_07_13_alexnet_cifar10_saprle_diff_level1_nopelog.txt`
- 命令使用：
  - `-is_sap_rle 1`
  - `-sap_threshold_file alexnet_cifar10/alexnet_sap_threshold.txt`
  - `-sap_level_table_file alexnet_cifar10/alexnet_sap_level_all_level1.txt`
  - `-pe_log 0`

### 3. 结果

- `accuracy = 1`
- `Stop reason = inference completed before -sim limit`
- `Total simulation time = 165255`
- `Total received packets = 23898`
- `Total received flits = 116507`
- `Global average delay = 6584.53`
- `Max delay = 27164`
- `Total energy = 1.24379e-05`
- `Total time = 6.55285 min`

### 4. 对照

- 旧全局 `d=256` SAP-RLE 日志：`bin/run_log/run_log_26_07_09_alexnet_cifar10_saprle_d256_after_v2change.txt`
  - `Total received flits = 135278`
  - `Global average delay = 7721.04`
  - `Total energy = 1.35684e-05`
  - `Total time = 6.45108 min`
- 差分阈值 level1 相比全局 `d=256`：
  - flit 减少 `18771`
  - 约减少 `13.88%`
  - 平均延迟降低 `1136.51 cycles`

### 5. 缓存与耗时确认

- 本次日志显示：
  - `Loaded convolution/pooling coordinates from cache`
  - `Loaded traffic cache format TRFCCHE1`
  - `Loaded Global Traffic Table from cache`
- 因此坐标缓存和通信缓存均已复用。
- 之前 `run_log_26_07_13_alexnet_cifar10_isapprox_after_fas_placeholder_sim220k_nopelog.txt` 耗时约 11.91 min 的主要原因是运行到了 `220001 cycles` 的仿真上限，而不是缓存未复用。

---

## 2026-07-13 AlexNet 当前 level table 双方案仿真

### 1. FAS/isapprox

- 使用文件：
  - `bin/alexnet_cifar10/alexnet_approx.txt`
  - `bin/alexnet_cifar10/alexnet_approx_level_table.txt`
- 日志：`bin/run_log/run_log_26_07_13_alexnet_cifar10_isapprox_current_level_table_nopelog.txt`
- 结果：
  - `accuracy = 1`
  - `Stop reason = reached -sim cycle limit or explicit stop condition`
  - `Total simulation time = 150721`
  - `Total received packets = 23898`
  - `Total received flits = 93497`
  - `Global average delay = 5203.63`
  - `Max delay = 21486`
  - `Total energy = 1.31196e-05`
  - `Total time = 12.8093 min`

### 2. SAP-RLE

- 使用文件：
  - `bin/alexnet_cifar10/alexnet_sap_threshold.txt`
  - `bin/alexnet_cifar10/alexnet_sap_level_table.txt`
- 日志：`bin/run_log/run_log_26_07_13_alexnet_cifar10_saprle_current_level_table_nopelog.txt`
- 结果：
  - `accuracy = 1`
  - `Stop reason = inference completed before -sim limit`
  - `Total simulation time = 163450`
  - `Total received packets = 23898`
  - `Total received flits = 112456`
  - `Global average delay = 6412.03`
  - `Max delay = 26446`
  - `Total energy = 1.21728e-05`
  - `Total time = 6.52315 min`

### 3. 缓存确认

- 两次日志均显示：
  - `Loaded convolution/pooling coordinates from cache`
  - `Loaded traffic cache format TRFCCHE1`
  - `Loaded Global Traffic Table from cache`

---

## 2026-07-13 AlexNet FAS/SAP-RLE 排查与 SAP-RLE-V2 验证

### 1. FAS/isapprox 自动停止异常

- 排查日志：`bin/run_log/run_log_26_07_13_alexnet_cifar10_isapprox_current_level_table_nopelog.txt`
- 现象：
  - 最后一层 `PE_52` 在同一个时间 `150455` 重复出现 `Now layer 11 start computing...`
  - `accuracy` 被重复累计，从 `right: 1` 一直累计到 `right: 11`
  - 最终没有触发 `[AutoStop]`
  - `Stop reason = reached -sim cycle limit or explicit stop condition`
- 判断：
  - 该 FAS 日志的 `Global average delay`、`Total energy` 和墙钟时间不适合作为可靠对比依据。
  - 当前问题更像是 FAS 接收恢复后最后层重复完成/重复计数导致的自动停止状态异常。

### 2. FAS placeholder 检查

- 源码中已存在 FAS 全近似包 placeholder：
  - `NoximFlit::is_fas_placeholder`
  - 全 BODY 被近似删除时插入一个 `FLIT_TYPE_BODY` placeholder
  - 接收端写回 `receive_data` 时跳过 `is_fas_placeholder`
- 结论：
  - 不是完全忘记加占位 flit。
  - 但 FAS 当前日志仍然存在最后层重复完成问题，需要后续单独修复后再作为论文对比数据使用。

### 3. SAP-RLE 与 FAS 当前结果解释

- 当前 FAS：
  - `Total received flits = 93497`
  - `Total energy = 1.31196e-05`
  - 没有 AutoStop，且最后层重复计数。
- 当前 SAP-RLE：
  - `Total received flits = 112456`
  - `Total energy = 1.21728e-05`
  - 正常 AutoStop。
- 判断：
  - 从 flit 数看，SAP-RLE 并没有低于 FAS，反而比 FAS 多 `18959` 个 flit。
  - SAP-RLE 能耗低于 FAS 的主要原因不能直接解释为网络更省，而是 FAS 日志存在重复完成和未自动停止问题，统计口径已经不干净。

### 4. SAP-RLE-V2 自动停止仿真

- 日志：`bin/run_log/run_log_26_07_13_alexnet_cifar10_saprle_v2_current_level_table_autostop_nopelog.txt`
- 使用命令关键参数：
  - `-is_sap_rle_v2 1`
  - `-sap_threshold_file alexnet_cifar10/alexnet_sap_threshold.txt`
  - `-sap_level_table_file alexnet_cifar10/alexnet_sap_level_table.txt`
  - `-stop_on_infer_done 1`
  - `-pe_log 0`
- 结果：
  - `accuracy = 1`
  - `Stop reason = inference completed before -sim limit`
  - `Total simulation time = 158595`
  - `Total received packets = 23898`
  - `Total received flits = 104570`
  - `Global average delay = 5987.27`
  - `Max delay = 25153`
  - `Total energy = 1.16894e-05`
  - `Total time = 6.6175 min`
- 缓存确认：
  - `Loaded convolution/pooling coordinates from cache`
  - `Loaded traffic cache format TRFCCHE1`
  - `Loaded Global Traffic Table from cache`

---

## 2026-07-13 FAS/isapprox 重复计算与 AutoStop 修复

### 1. 备份

- 备份目录：`backups/2026-07-13_fas_autostop_duplicate_compute_before_fix/`
- 已备份：
  - `src/NoximProcessingElement.cpp`
  - `修改日志.md`

### 2. 问题

- AlexNet FAS/isapprox 日志中最后一层出现重复完成：
  - `PE_52` 在同一个时间 `150455` 多次进入 layer 11 计算。
  - `accuracy` 从 `right: 1` 重复累计到 `right: 11`。
  - 没有触发 `[AutoStop]`，最终跑到 `-sim` 上限。
- 判断原因：
  - 接收端使用 `should_receive[wz] == 0` 作为计算触发条件。
  - 当后续包尾或恢复流程再次进入该判断时，已经完成过计算的图片仍可能重复进入计算路径。

### 3. 改动

- 在 `src/NoximProcessingElement.cpp` 中将计算触发条件从：

```cpp
if (should_receive[wz] == 0)
```

改为：

```cpp
if (should_receive[wz] == 0 && flag_f[wz] == 0)
```

- 含义：
  - 只有“本图片数据已接收完整”且“本图片在当前 PE 尚未完成计算”时，才进入计算。
  - 防止 FAS/isapprox 恢复包后续事件重复触发最后层计算和 accuracy 统计。

### 4. LeNet5 验证

- 编译：`make -C bin` 通过。
- 验证日志：`bin/run_log/run_log_26_07_13_lenet5_isapprox_duplicate_compute_fix_nopelog.txt`
- 结果：
  - `accuracy = 1`
  - `accuracy:` 出现次数 = `1`
  - `Now layer 7 start computing` 出现次数 = `1`
  - `[AutoStop] inference completed at cycle 12462`
  - `Stop reason = inference completed before -sim limit`
  - `Total received packets = 1547`
  - `Total received flits = 6298`
  - `Global average delay = 591.955`
  - `Total energy = 4.78096e-07`

### 5. 后续

- 需要重新运行 AlexNet FAS/isapprox，确认修复后 AlexNet 是否也能正常 AutoStop，并重新生成可用于对比的 FAS 网络统计。

---

## 2026-07-13 VGG16 SAP-RLE 差分阈值表生成

### 1. 新增脚本

- 新增 `VGG16_CIFAR10/stat_vgg16_sap_diff_quantiles.py`。
- 默认读取：
  - `VGG16_CIFAR10/analysis_csv/quantized_layer_features_long.csv`
  - `bin/vgg16_cifar10/model_new.txt`
- 使用已有 `value_q16` 量化激活值统计每层相邻激活值的绝对差分。
- 默认只统计同号相邻数据的差分，以匹配 SAP-RLE 当前 `same_sign && abs_diff <= delta` 的压缩条件。
- 去除 0 差分后排序，使用 nearest-rank 位置取 20%、40%、60%、80% 四个阈值。
- 由于当前默认 Python 环境没有 `torch`，脚本改为无 PyTorch 依赖版本，直接读取已有量化激活 CSV。

### 2. 生成文件

- 阈值表：`bin/vgg16_cifar10/vgg16_sap_threshold.txt`
- level 表：`bin/vgg16_cifar10/vgg16_sap_level_table.txt`
- 明细 CSV：`VGG16_CIFAR10/analysis_csv/vgg16_sap_diff_quantiles.csv`
- 摘要文档：`VGG16_CIFAR10/analysis_csv/vgg16_sap_diff_quantiles.md`

### 3. 生成结果

```text
Convolution 200 521 1179 2844
Convolution 304 744 1523 3293
Pooling 415 1038 2140 4407
Convolution 691 1586 2861 5020
Convolution 728 1633 2860 5019
Pooling 1068 2309 3956 6564
Convolution 1033 2191 3599 5884
Convolution 1066 2343 3949 6332
Convolution 841 1828 3044 5005
Pooling 1229 2547 4210 6591
Convolution 1110 2355 3851 6087
Convolution 1173 2522 4228 6863
Convolution 914 1993 3449 5622
Pooling 1395 2959 5139 8465
Convolution 1670 3834 6297 10430
Convolution 1682 3658 6786 12760
Convolution 1207 2768 5648 13055
Pooling 3063 6663 11781 18616
Dense 1849 2237 5050 5659
```

### 4. 验证

- `python3 VGG16_CIFAR10/stat_vgg16_sap_diff_quantiles.py` 执行成功。
- `python3 -m py_compile VGG16_CIFAR10/stat_vgg16_sap_diff_quantiles.py` 通过。
- `bin/vgg16_cifar10/vgg16_sap_threshold.txt` 共 19 行。
- `bin/vgg16_cifar10/vgg16_sap_level_table.txt` 共 19 行。
- 两个文件行数均与 VGG16 的 19 个非输入层对应。

---

## 2026-07-13 AlexNet FAS 修复后仿真结果确认

### 1. 日志

- 日志：`bin/run_log/run_log_26_07_13_alexnet_cifar10_isapprox_current_level_table_after_dupfix_nopelog.txt`

### 2. 修复状态

- `accuracy:` 出现次数 = `1`
- `Now layer 11 start computing` 出现次数 = `1`
- `[AutoStop] inference completed at cycle 150455`
- `Stop reason = inference completed before -sim limit`
- 判断：FAS/isapprox 最后一层重复计算和不自动停止问题已在 AlexNet 上消除。

### 3. 结果

- `accuracy = 1`
- `Total simulation time = 150721`
- `Total received packets = 23898`
- `Total received flits = 93497`
- `Global average delay = 5203.63`
- `Global average throughput = 0.150748`
- `Throughput/IP = 0.0114862`
- `Max delay = 21486`
- `Total energy = 9.92376e-06`
- `Total time = 6.35691 min`

### 4. 与 SAP-RLE/SAP-RLE-V2 对比

| 方案 | Total flits | Avg delay | Max delay | Energy | AutoStop |
| --- | ---: | ---: | ---: | ---: | --- |
| FAS/isapprox 修复后 | 93497 | 5203.63 | 21486 | 9.92376e-06 | 是 |
| SAP-RLE | 112456 | 6412.03 | 26446 | 1.21728e-05 | 是 |
| SAP-RLE-V2 | 104570 | 5987.27 | 25153 | 1.16894e-05 | 是 |

- 当前 AlexNet 配置下，修复后的 FAS/isapprox 在 flit 数、平均延迟、最大延迟和能耗上均优于 SAP-RLE 与 SAP-RLE-V2。

---

## 2026-07-13 FAS/isapprox placeholder 触发条件修正

### 1. 备份

- 备份目录：`backups/2026-07-13_fas_placeholder_half_payload_before_change/`
- 已备份：
  - `src/NoximProcessingElement.cpp`
  - `修改日志.md`

### 2. 问题

- 原实现中，FAS/isapprox 只有在一个包内全部 BODY flit 都被近似删除时，才额外插入 placeholder BODY flit。
- 该理解不准确。
- 按 FAS 论文思路，额外 flit 表示近似元数据开销；论文中提到的 `8` 是特定 flit 位宽和特定载荷数量下的结果，不应写死为“全包近似”条件。

### 3. 改动

- 将 placeholder 触发条件改为：

```cpp
fas_body_count > 0 && fas_erased_body_count * 2 > fas_body_count
```

- 含义：
  - 原始 BODY flit 数为 `fas_body_count`
  - 被 FAS 近似删除的 BODY flit 数为 `fas_erased_body_count`
  - 当被近似删除的数据超过原始 payload 的一半时，额外插入 1 个 placeholder BODY flit。
- 正好等于一半时不插入，严格对应“超过一半”。
- 同时将 placeholder 的 `sequence_no` 从固定 `1` 改为 `original_flit_vector_size - 1`，避免在部分 BODY 仍保留时和真实 BODY flit 的序号重叠。

### 4. 验证

- 编译：`make -C bin` 通过。
- 后续需要重新运行 AlexNet FAS/isapprox，生成修正 placeholder 条件后的对比日志。

### 5. AlexNet 重新仿真结果

- 日志：`bin/run_log/run_log_26_07_13_alexnet_cifar10_isapprox_half_payload_placeholder_nopelog.txt`
- 结果：
  - `accuracy = 1`
  - `accuracy:` 出现次数 = `1`
  - `Now layer 11 start computing` 出现次数 = `1`
  - `[AutoStop] inference completed at cycle 155319`
  - `Stop reason = inference completed before -sim limit`
  - `Total simulation time = 155585`
  - `Total received packets = 23898`
  - `Total received flits = 100977`
  - `Global average delay = 5626.06`
  - `Max delay = 22897`
  - `Total energy = 1.0379e-05`
  - `Total time = 6.19922 min`

### 6. 对比

| 方案 | Total flits | Avg delay | Max delay | Energy |
| --- | ---: | ---: | ---: | ---: |
| FAS 旧 placeholder 条件 | 93497 | 5203.63 | 21486 | 9.92376e-06 |
| FAS half-payload placeholder | 100977 | 5626.06 | 22897 | 1.0379e-05 |
| SAP-RLE-V2 | 104570 | 5987.27 | 25153 | 1.16894e-05 |
| SAP-RLE | 112456 | 6412.03 | 26446 | 1.21728e-05 |

- half-payload placeholder 条件使 FAS 比旧条件增加 `7480` 个 flit。
- 当前 AlexNet 配置下，FAS half-payload placeholder 仍低于 SAP-RLE-V2 `3593` 个 flit，低于 SAP-RLE `11479` 个 flit。

---

## 2026-07-13 计算能耗统计修正：0 输入也计入精确计算能耗

### 1. 备份

- 备份目录：`backups/2026-07-13_compute_energy_count_zero_before_change/`
- 已备份：
  - `src/NoximProcessingElement.cpp`
  - `修改日志.md`

### 2. 问题

- 原实现中，多处计算能耗统计使用 `if (input_val != 0) stats.power.compute(...)`。
- 这相当于把输入为 `0` 的乘加视作不耗能，口径更接近“稀疏计算/跳 0 计算”。
- 当前实验要求为全精确计算，不启用稀疏计算，因此输入为 `0` 时也应统计 MAC 能耗。

### 3. 改动

- 修改 `src/NoximProcessingElement.cpp` 中全连接层、卷积层、输入层直算路径和 TDM reset 路径的计算能耗统计。
- 默认精确计算 `approx_compute == 0` 下，不再跳过 `input_val == 0` 的能耗统计。
- `BIASED_MUL` / `UNBIASED_MUL` 的低值 shift 分支也不再因为 `input_val == 0` 跳过能耗统计。
- 显式稀疏计算模式 `approx_compute == 6` 仍保留原有跳 0 能耗语义，避免破坏专门测试稀疏计算的功能。

### 4. 纯网络能耗口径说明

- 当前全局日志中的 `Total energy` 来自 `router->getPower() + pe->getPower()`，不是纯通信能耗。
- `NoximPower` 内部有 `pwr_nn_router`、`pwr_nn_mem`、`pwr_nn_pe` 三类累计值，并通过 `getRouterPower()`、`getMEMPower()`、`getComputePower()` 分别返回。
- 但当前 `NoximGlobalStats::showStats()` 没有单独输出纯网络能耗字段。
- 当前 `NoximRouter::getPower()` 返回 `router + mem`，`NoximProcessingElement::getPower()` 返回 `compute`，全局 `Total energy` 是二者之和。

---

## 2026-07-13 日志新增网络能耗与计算能耗拆分输出

### 1. 备份

- 备份目录：`backups/2026-07-13_network_energy_output_before_change/`
- 已备份：
  - `src/NoximGlobalStats.cpp`
  - `src/NoximGlobalStats.h`
  - `修改日志.md`

### 2. 改动

- 新增 `NoximGlobalStats::getNetworkPower()`：
  - 逐 tile 累加 `noc->t[x][y][z]->r->getPower()`。
  - 当前口径等价于 `router + router_mem`，因为 `NoximRouter::getPower()` 返回 `stats.power.getRouterPower() + stats.power.getMEMPower()`。
- 新增 `NoximGlobalStats::getComputePower()`：
  - 逐 tile 累加 `noc->t[x][y][z]->pe->getPower()`。
  - 当前口径为 PE 计算能耗。
- `showStats()` 保留原有 `% Total energy (J)`，并额外输出：
  - `% Network energy (router+router_mem, J)`
  - `% Compute energy (PE, J)`

### 3. 目的

- 便于比较 FAS、SAP-RLE、SAP-RLE-V2 等近似通信方案时，单独观察通信侧能耗，避免被 PE 计算能耗混淆。

---

## 2026-07-13 AlexNet 10 图输入与阈值表导出

### 1. 备份

- 备份目录：`backups/2026-07-13_alexnet_10img_export_before_debug_print_change/`
- 已备份：
  - `src/NoximProcessingElement.cpp`
  - `修改日志.md`

### 2. 新增脚本

- 新增 `AlenNet_CIFAR10/export_alexnet_cifar10_10img_thresholds.py`。
- 输入目录：`CIFAR10/`。
- 复用现有 AlexNet Noxim 文件：
  - `bin/alexnet_cifar10/alexnet_model.txt`
  - `bin/alexnet_cifar10/alexnet_weight_fc_wb2parser.txt`

### 3. 生成文件

- `bin/alexnet_cifar10/alexnet_input_10img.txt`
- `bin/alexnet_cifar10/alexnet_label_10img.txt`
- `bin/alexnet_cifar10/alexnet_approx_10img.txt`
- `bin/alexnet_cifar10/alexnet_approx_level_table_10img.txt`
- `bin/alexnet_cifar10/alexnet_sap_threshold_10img.txt`
- `bin/alexnet_cifar10/alexnet_sap_level_table_10img.txt`
- 额外摘要：`bin/alexnet_cifar10/alexnet_10img_threshold_summary.txt`

### 4. 文件检查

- `alexnet_input_10img.txt`：`960` 行，对应 `10` 张 CIFAR-10 图片，每张 `96` 行。
- `alexnet_label_10img.txt`：`10` 行，标签为 `3 8 0 6 1 5 7 4 2 9`。
- FAS/SAP 阈值表和 level 表均为 `11` 行，对应 AlexNet 的 `11` 个非输入层。
- FAS 阈值文件按 cnn-noxim 读取逻辑写入为 `q80 q60 q40 q20`，读取后内部顺序为 `q20 q40 q60 q80`。
- 按现有单图模型 `input_scale` 量化 10 张图片时，记录到 `input_clipped_values=568`。

### 5. 调试打印清理

- 注释 `src/NoximProcessingElement.cpp` 中卷积分支的阈值调试打印：
  - `c NN_Model->all_layer_approx[...]`
- 其它日志打印保留。

---

## 2026-07-13 AlexNet SAP corrected level table 仿真

### 1. corrected SAP-RLE

- 日志：`bin/run_log/run_log_26_07_13_alexnet_cifar10_saprle_corrected_level_table_nopelog.txt`
- 使用：
  - `bin/alexnet_cifar10/alexnet_sap_threshold.txt`
  - `bin/alexnet_cifar10/alexnet_sap_level_table.txt`
- 结果：
  - `accuracy = 1`
  - `accuracy:` 出现次数 = `1`
  - `Now layer 11 start computing` 出现次数 = `1`
  - `Stop reason = inference completed before -sim limit`
  - `Total simulation time = 162178`
  - `Total received packets = 23898`
  - `Total received flits = 110286`
  - `Global average delay = 6317.23`
  - `Max delay = 26446`
  - `Total energy = 1.20198e-05`
  - `Total time = 6.12378 min`

### 2. corrected SAP-RLE-V2

- 日志：`bin/run_log/run_log_26_07_13_alexnet_cifar10_saprle_v2_corrected_level_table_nopelog.txt`
- 使用：
  - `bin/alexnet_cifar10/alexnet_sap_threshold.txt`
  - `bin/alexnet_cifar10/alexnet_sap_level_table.txt`
- 结果：
  - `accuracy = 1`
  - `accuracy:` 出现次数 = `1`
  - `Now layer 11 start computing` 出现次数 = `1`
  - `Stop reason = inference completed before -sim limit`
  - `Total simulation time = 156955`
  - `Total received packets = 23898`
  - `Total received flits = 101882`
  - `Global average delay = 5872.1`
  - `Max delay = 25153`
  - `Total energy = 1.1507e-05`
  - `Total time = 6.14009 min`

### 3. 与 FAS half-payload placeholder 对比

| 方案 | Total flits | Avg delay | Max delay | Energy |
| --- | ---: | ---: | ---: | ---: |
| FAS half-payload placeholder | 100977 | 5626.06 | 22897 | 1.0379e-05 |
| corrected SAP-RLE-V2 | 101882 | 5872.1 | 25153 | 1.1507e-05 |
| corrected SAP-RLE | 110286 | 6317.23 | 26446 | 1.20198e-05 |

- corrected SAP-RLE-V2 相比旧 SAP-RLE-V2 的 flit 数从 `104570` 降到 `101882`。
- corrected SAP-RLE 相比旧 SAP-RLE 的 flit 数从 `112456` 降到 `110286`。
- corrected SAP-RLE-V2 已非常接近 FAS half-payload placeholder，flit 只多 `905`，但平均延迟、最大延迟和能耗仍略高。

## 2026-07-13 关闭近似阈值调试打印

- 修改文件：`src/NoximProcessingElement.cpp`
- 备份目录：`backups/2026-07-13_disable_threshold_debug_prints/`
- 修改内容：
  - 注释全连接层发送阶段的 `f NN_Model->all_layer_approx...` 阈值打印。
  - 确认卷积层阈值打印保持关闭，并注释卷积层发送阶段遗留的 `c approx: end` 调试打印。
  - 注释池化层发送阶段的 `p NN_Model->all_layer_approx...` 阈值打印。
- 目的：减少多 PE 仿真时重复阈值日志刷屏，不改变阈值读取、包生成和近似通信逻辑。

## 2026-07-13 关闭额外传输调试打印

- 修改文件：`src/NoximProcessingElement.cpp`
- 备份目录：`backups/2026-07-13_disable_more_debug_prints/`
- 修改内容：
  - 注释 Conv->Pool 分支中的 `Conv layer ... next is Pool, trans_conv.size()=...` 和 `trans_PE_ID.size()=...` 普通调试打印。
  - 注释池化层分支末尾的 `pool end` 普通调试打印。
- 保留内容：
  - 保留 `ERROR: trans_conv is empty!` 等异常提示，便于发现传输表初始化失败。
- 目的：减少连续多图片推理时的日志噪声，不改变传输表构造和仿真行为。

## 2026-07-13 重生成 VGG16-CIFAR10 FAS 阈值表

- 新增脚本：`VGG16_CIFAR10/stat_vgg16_fas_quantiles.py`
- 备份目录：`backups/2026-07-13_vgg16_fas_threshold_regen/`
- 输入：
  - 模型权重：`VGG16_CIFAR10/best_model_fused.pth`
  - 推理图片：`VGG16_CIFAR10/idx_01824_label_truck.png`
  - 量化 scale 来源：`bin/vgg16_cifar10/model_new.txt`
- 输出：
  - 更新 `bin/vgg16_cifar10/approx.txt`
  - 更新 `bin/vgg16_cifar10/approx_level_table.txt`
  - 生成 `VGG16_CIFAR10/analysis_csv/vgg16_fas_nonzero_quantiles.csv`
  - 生成 `VGG16_CIFAR10/analysis_csv/vgg16_fas_nonzero_quantiles.md`
- 说明：
  - 阈值来自单张 truck 图片逐层推理后的 int16 量化激活非零值 q20/q40/q60/q80。
  - `approx.txt` 按 `q80 q60 q40 q20` 写入，以匹配 cnn-noxim 读取时反向存储阈值的实现。
  - `approx_level_table.txt` 每层写入 `0 1 2 3 3 3`，默认 `config_sel=0` 使用 q20。

## 2026-07-13 补齐 VGG11-CIFAR10 标准文件

- 新增脚本：`VGG11_CIFAR10/export_vgg11_cifar10_10img_thresholds.py`
- 备份目录：`backups/2026-07-13_vgg11_standard_files/`
- 参考标准：`bin/alexnet_cifar10/` 的单张图文件、10 张图文件、FAS 阈值表、SAP-RLE 阈值表和 level 表命名方式。
- 更新/生成文件：
  - `bin/vgg11_cifar10/vgg11_approx.txt`
  - `bin/vgg11_cifar10/vgg11_approx_level_table.txt`
  - `bin/vgg11_cifar10/vgg11_sap_threshold.txt`
  - `bin/vgg11_cifar10/vgg11_sap_level_table.txt`
  - `bin/vgg11_cifar10/vgg11_sap_level_all_level1.txt`
  - `bin/vgg11_cifar10/vgg11_input_10img.txt`
  - `bin/vgg11_cifar10/vgg11_label_10img.txt`
  - `bin/vgg11_cifar10/vgg11_approx_10img.txt`
  - `bin/vgg11_cifar10/vgg11_approx_level_table_10img.txt`
  - `bin/vgg11_cifar10/vgg11_sap_threshold_10img.txt`
  - `bin/vgg11_cifar10/vgg11_sap_level_table_10img.txt`
  - `bin/vgg11_cifar10/vgg11_10img_threshold_summary.txt`
- 说明：
  - 单张图阈值来自 `VGG11_CIFAR10/idx_01824_label_truck.png`。
  - 10 张图阈值来自 `CIFAR10/` 下 10 张代表图片。
  - FAS 阈值使用每层 int16 量化激活非零值的 q20/q40/q60/q80，并按 `q80 q60 q40 q20` 写入。
  - SAP-RLE 阈值使用每层 int16 量化激活非零相邻差分值的 q20/q40/q60/q80。
  - 未重新生成 `.coord_cache` 和 `.traffic_cache`，因为这类缓存由 cnn-noxim 仿真阶段生成。

## 2026-07-14 修复 VGG16 SAP-RLE-V2 非 FAS 模式空阈值表崩溃

- 备份文件：`src/NNModel.cpp.bak_20260714_vgg16_sap_crash_fix`
- 修改文件：`src/NNModel.cpp`
- 问题：
  - SAP-RLE/SAP-RLE-V2、exact、ABDTR 等非 FAS 模式没有必要读取 FAS 阈值表，但发包路径仍会携带 `approx_threshold` 元数据。
  - 当未指定 `-NNapprox/-NNapprox_Level_Table` 时，`all_layer_approx`、`all_layer_approx_level_table`、`each_layer_num` 可能为空，VGG16 SAP-RLE-V2 在第 1 层首次发包时触发越界并段错误。
- 修复：
  - FAS 模式下继续严格检查阈值表和 level 表层数必须与模型非输入层数一致。
  - 非 FAS 模式下，如果 FAS 阈值表或 level 表未加载/层数不匹配，则自动补齐每层 4 个 0 阈值和 6 个 0 level。
  - 独立补齐 `each_layer_num`，避免它依赖 FAS 阈值文件初始化。
- 影响范围：
  - 不改变 FAS 方案的真实阈值读取。
  - 只为非 FAS 方案提供安全默认元数据，避免空表越界。

## 2026-07-14 添加 VGG16 FAS 第 4 层接收调试打印

- 备份文件：`src/NoximProcessingElement.cpp.bak_20260714_fas_layer4_debug`
- 修改文件：`src/NoximProcessingElement.cpp`
- 背景：
  - 旧 VGG16 exact 日志能在 `4498830` cycles 完整推理并自动停止。
  - 当前 VGG16 FAS 日志只到第 3 层开始发送，第 4 层没有开始计算，说明第 4 层接收条件没有达成。
- 调试内容：
  - 仅在 `-isapprox 1` 且 `ID_layer == 4` 时启用。
  - 打印标签：
    - `[FAS_DEBUG_LAYER4_PACKET]`：完整包到达后，输出恢复后 body 数、期望 body 数、placeholder 数、approx_count、当前 `should_receive`。
    - `[FAS_DEBUG_LAYER4_UNMATCHED]`：恢复出的 body flit 的 `src_Neu_id` 无法匹配当前 PE 的 `receive_Neu_ID` 时输出。
- 目的：
  - 判断 FAS 卡在第 4 层的原因是包恢复 body 数量不对，还是恢复出的 `src_Neu_id` 与接收表不匹配。

## 2026-07-14 添加 FAS 池化层最小调试打印

- 备份目录：`backups/2026-07-14_fas_pool_min_debug/`
- 修改文件：`src/NoximProcessingElement.cpp`
- 背景：
  - VGG16 FAS 在 Pooling -> Convolution 阶段没有进入下一层计算。
  - LeNet5 FAS 短仿真也疑似卡在 Pooling -> Convolution。
- 调试内容：
  - `[FAS_DEBUG_POOL_TX]`：池化层发包前打印 `trans_PE_ID/trans_pool/trans_PE_ID_pool` 的规模和首个发送目标。
  - `[FAS_DEBUG_POOL_NEXTFLIT]`：池化层 `nextFlit()` 分支打印每个 body flit 的 `src_Neu_id/data/start_index` 变化。
  - `[FAS_DEBUG_PLACEHOLDER_TX]`：FAS 发送端插入占位 body flit 时打印占位标记、包长和删除 body 数。
  - `[FAS_DEBUG_PLACEHOLDER_RX]`：接收端完整包到达后打印占位 body 标记是否仍然存在，以及是否出现 `src_Neu_id < 0` 的 body。
- 影响范围：
  - 只增加限量调试打印，不改变发包、占位 flit、恢复或计算逻辑。

## 2026-07-14 添加 LeNet5 FAS 第 3 层接收调试打印

- 备份目录：`backups/2026-07-14_fas_layer3_rx_debug/`
- 修改文件：`src/NoximProcessingElement.cpp`
- 背景：
  - 完整 clean rebuild 后，FAS placeholder 标记在接收端已经能保持为 `placeholder_true=1`。
  - LeNet5 FAS 最小仿真仍停在第 2 层 Pooling 发送后，第 3 层 Convolution 没有开始计算。
- 调试内容：
  - `[FAS_DEBUG_LAYER3_PACKET]`：第 3 层完整包到达后打印恢复后的 body 数、期望 body 数、placeholder 数、approx_count、`should_receive`。
  - `[FAS_DEBUG_LAYER3_UNMATCHED]`：第 3 层接收端无法匹配 `src_Neu_id` 时打印该 flit 信息。
  - `[FAS_DEBUG_LAYER3_PROGRESS]`：第 3 层接收接近完成时打印 `should_receive` 下降过程。
- 影响范围：
  - 只增加限量调试打印，不改变发包、placeholder、恢复或计算逻辑。

## 2026-07-14 修复 FAS packet.isapprox 未传递到 flit 的问题

- 备份目录：`backups/2026-07-14_fas_isapprox_flag_fix/`
- 修改文件：`src/NoximProcessingElement.cpp`
- 问题：
  - `nextFlit()` 中 `flit.isapprox = packet.isapprox;` 被注释，导致 FAS/isapprox 包生成 flit 时没有继承 packet 的近似标记。
  - 接收端以 `flit.isapprox` 判断是否进入 FAS 恢复路径，部分包可能被当作普通包处理，造成被近似删除的 body flit 无法恢复。
- 修复：
  - 恢复 `flit.isapprox = packet.isapprox;`。
- 验证计划：
  - 使用 LeNet5 FAS 最小仿真检查 Layer 2 是否收齐并开始发送、Layer 3 是否进入计算。

## 2026-07-14 添加 FAS 恢复分支跳过调试打印

- 备份目录：`backups/2026-07-14_fas_recovery_skip_debug/`
- 修改文件：`src/NoximProcessingElement.cpp`
- 背景：
  - 恢复 `flit.isapprox = packet.isapprox;` 后，LeNet5 FAS 仍出现 `HEAD + placeholder BODY + TAIL` 的小包未补回真实 body flit。
  - 需要确认 tail 到达时是否进入了 FAS 近似恢复分支。
- 调试内容：
  - `[FAS_DEBUG_RECOVERY_SKIPPED]`：当 `-isapprox 1` 已启用、包头携带近似位置或包内有 placeholder，但当前 tail 没有进入近似恢复分支时打印。
  - `[FAS_DEBUG_LAYER3_PACKET]` 补充 `head_isapprox/tail_isapprox`，用于检查 packet/flit 近似标志在网络传输后是否保持。
- 影响范围：
  - 只增加限量调试打印，不改变发包、placeholder、恢复或计算逻辑。

## 2026-07-14 修复 FAS placeholder 导致近似包标记失效的问题

- 备份目录：`backups/2026-07-14_fas_mark_by_deleted_body/`
- 修改文件：`src/NoximProcessingElement.cpp`
- 问题：
  - 原逻辑使用 `flit_vector.size() != original_flit_vector_size` 判断 FAS 包是否为近似包。
  - 引入 FAS placeholder body flit 后，若原包只有 1 个 body flit，该 body 被近似删除后又插入 1 个 placeholder，最终包长会恢复为原始包长。
  - 这会导致包头已携带 `approx_pos`，但整包 `isapprox=0`，接收端跳过恢复分支。
- 修复：
  - 新增 `fas_packet_has_approx_data`，只在 FAS 压缩阶段根据 `fas_erased_body_count > 0` 置位。
  - FAS 包的 `isapprox` 标记改为依据是否实际删除过 body flit，而不是依据最终包长是否变化。
- 影响范围：
  - 该变量只由 FAS/isapprox 压缩代码块置位。
  - ABDTR、SAP-RLE、SAP-RLE-V2 在正常互斥命令下不受影响。

## 2026-07-14 关闭 FAS 调试打印

- 备份目录：`backups/2026-07-14_disable_fas_debug_prints/`
- 修改文件：`src/NoximProcessingElement.cpp`
- 背景：
  - FAS placeholder 相关问题已通过 LeNet5 和 AlexNet 验证。
  - `[FAS_DEBUG_*]` 调试打印会显著增加 run_log 体积。
- 修改：
  - 新增统一调试开关 `kEnableFasDebugPrints=false`。
  - 关闭 `[FAS_DEBUG_RECOVERY_SKIPPED]`、`[FAS_DEBUG_LAYER3_PACKET]`、`[FAS_DEBUG_LAYER4_PACKET]`、`[FAS_DEBUG_PLACEHOLDER_RX/TX]`、`[FAS_DEBUG_POOL_TX]`、`[FAS_DEBUG_POOL_NEXTFLIT]` 等临时调试输出。
- 影响范围：
  - 只关闭调试打印，不改变 FAS、ABDTR、SAP-RLE、SAP-RLE-V2 的通信和恢复逻辑。

## 2026-08-24 修复 tanh 网络下 max-pooling 全负窗口错误

- 备份文件：`backups/NoximProcessingElement.cpp.bak_20260824_tanh_pool_max_fix`
- 修改文件：`src/NoximProcessingElement.cpp`
- 问题：
  - 原 max-pooling 将初始最大值设为 `0`。
  - ReLU 网络输出非负，因此该问题不会暴露。
  - tanh 网络输出可能为负，当池化窗口内所有输入均为负数时，原逻辑会错误输出 `0`，而不是窗口中的最大负值。
- 修复：
  - 为每个池化输出新增 `max_pool_initialized` 标志。
  - max-pooling 使用第一个有效输入初始化最大值，之后再进行大小比较。
  - 同时关闭之前用于 tanh 精确路径定位的 `kEnableTanhExactDebugPrints`。
- 验证：
  - ReLU 版 AlexNet 精确仿真日志：`bin/run_log/run_log_26_08_24_alexnet_cifar10_relu_exact_after_maxpool_fix_nopelog.txt`
  - ReLU 验证结果与历史日志 `bin/run_log/run_log_26_07_14_alexnet_cifar10_exact_single_nopelog.txt` 一致：预测 `9`，accuracy `1`，packets `23898`，flits `190900`，Global average delay `10919.6`，Total energy `2.28643e-05`。
  - Tanh 版 AlexNet 精确仿真日志：`bin/run_log/run_log_26_08_24_alexnet_cifar10_tanh_exact_after_maxpool_fix_nopelog.txt`
  - Tanh 验证结果：预测 `9`，label `9`，accuracy `1`，输出向量为 `[9451, 4683, -6615, -1333, -10580, -1331, -13908, -10868, -443, 32764]`。
