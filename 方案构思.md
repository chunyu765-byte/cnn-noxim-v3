# 通信近似新方案构思（以减少 flit 个数为目标）

## 1. 背景与目标
当前项目里已经有几类通信近似（例如阈值近似、ABDTR、全零包、drop/trunc）。

这里给出**额外可选**的通信方案，核心目标是：
- 降低 `Total received flits`
- 在可控精度损失（或无损）下减少网络带宽压力
- 与当前 `txProcess/rxProcess` 架构兼容

---

## SAP-RLE 方案（已实现）

本节描述当前项目内已经落地的 `SAP-RLE`（Sign-Aware Anchor RLE）方案，包括构思与实现。

### 1. 设计动机

- 在不改 NoC 路由和 flit 基本结构的前提下，减少包内 `BODY` 数量。
- 复用现有“HEAD 携带近似位置信息、TAIL 到达后统一恢复”的机制，降低改动风险。
- 对 CNN 层间激活（大量局部相近/重复值）实现低成本压缩。

### 2. 核心思想（Anchor + Sign-Aware Delta）

对单个 packet 的 `BODY` 序列按顺序处理：

1. 第一个 `BODY` 作为 anchor，保留发送。
2. 后续 `BODY` 若满足：
   - `same_sign == true`
   - `|cur - anchor| <= delta`
   则该 flit 不发送（drop），仅在 `HEAD` 记录其位置与源神经元 id。
3. 若不满足条件，则该值保留发送，并更新 `anchor = cur`。

其中 `delta` 由命令行参数 `-sap_rle_delta` 指定。

### 3. 接收端恢复策略（当前实现）

在 `TAIL` 到达后按 `HEAD` 中记录的位置恢复缺失 flit：

1. 优先取“左侧最近有效 `BODY`”值。
2. 若左侧不存在，则取“右侧最近有效 `BODY`”值。
3. 若左右都不存在，则置 `0`。

这与 SAP-RLE 的近邻恢复思路一致，硬件实现简单（不需要复杂插值或额外乘加）。

### 4. 参数与模式约束

- 开关：`-is_sap_rle 1`
- 阈值：`-sap_rle_delta N`（`N >= 0`）
- 互斥：SAP-RLE 与以下近似通信模式互斥（解析器强制检查）
  - `-isapprox 1`（阈值近似）
  - `-allzeropacket 1`
  - `-acdc_abdtr 1`
  - `-is_drop_trunc 1`

### 5. 特殊情况说明

- `delta = 0`：
  - 仅会压缩“与 anchor 完全相等且同号”的后续值。
  - 对这类重复值恢复后与原值一致，可视为该部分无损。
  - 但对“不相等值”不会压缩，因此整体压缩率取决于包内重复度。

- 包内跨行/跨通道：
  - SAP-RLE 不依赖二维空间信息，仅按 packet 中 `BODY` 的线性顺序工作。
  - 与当前映射/打包逻辑兼容。

### 6. 代码实现落点

- 命令行参数定义与解析：
  - `src/NoximMain.h`：`is_sap_rle`、`sap_rle_delta`
  - `src/NoximCmdLineParser.cpp`：参数帮助、解析、合法性与互斥检查

- 发送端压缩：
  - `src/NoximProcessingElement.cpp`：`txProcess` 中 SAP-RLE 分支
  - 通过 `flit_vector[0].approx_pos / approx_src_id / approx_level` 在 `HEAD` 记录恢复元信息

- 接收端恢复：
  - `src/NoximProcessingElement.cpp`：`rxProcess` 在 `TAIL` 到达后按队列恢复缺失 BODY
  - SAP-RLE 分支使用“左邻优先，其次右邻，否则0”的恢复规则

### 7. 一条可直接运行的示例命令

```bash
./noxim -dimx 8 -dimy 8 -dimz 1 \
  -NNmodel lenet5/model_new.txt \
  -NNweight lenet5/weight_fc_wb2parser.txt \
  -NNweight_scale lenet5/weight_scale.txt \
  -NNinput lenet5/input7_new.txt \
  -NNapprox lenet5/approx.txt \
  -NNapprox_Level_Table lenet5/approx_level_table.txt \
  -NNlabel lenet5/label7.txt \
  -mapping dir_x -groupsize 1024 -sim 40000 \
  -isapprox 0 -allzeropacket 0 -acdc_abdtr 0 -is_drop_trunc 0 \
  -is_sap_rle 1 -sap_rle_delta 0
```

---

## 2. 候选方案总览

| 方案 | 是否有损 | 预计减flit能力 | 实现复杂度 | 适用场景 |
|---|---|---:|---:|---|
| A. 稀疏位图压缩（SBC） | 可无损（针对0） | 高（稀疏层可明显下降） | 中 | ReLU后中间层，0很多 |
| B. 多值打包（Multi-Value Packing） | 可无损/有损 | 中-高 | 中 | 定点/低比特激活 |
| C. 块级Top-K传输（BTK） | 有损 | 高 | 中 | 允许精度换流量 |
| D. Delta+RLE时序压缩（DRLE） | 可近无损 | 中 | 中-高 | 激活相邻相关性强 |
| E. 预测残差门控（PEG） | 有损/近无损 | 中-高 | 高 | 层内平滑、可预测 |
| F. 多播目的合并（Multicast） | 无损 | 高（全网总flit） | 高（需路由支持） | 一发多收场景 |

---

## 3. 方案细节与实现方式

## A. 稀疏位图压缩（SBC, Sparse Bitmap Compression）

### 思路
每个包不再发送所有 body flit。只发送非零（或大于门限）值，同时在头部携带“位置位图”。

### 为什么能减flit
若原始 body 数是 `N`，非零数是 `nnz`，则发送体量约从 `N` 变为 `nnz + 位图开销`。在 ReLU 后通常 `nnz << N`。

### 实现方式
1. 发送端（`NoximProcessingElement::txProcess`）
- 生成原始 `flit_vector` 后，扫描 body。
- 记录保留位置（位图）+ 保留值。
- 头flit增加字段：`comm_mode=SBC`、`orig_body_count`、`bitmap`。
- 仅把保留值推进 `flit_queue`。

2. 接收端（`NoximProcessingElement::rxProcess`）
- 在 tail 到达时整包恢复。
- 根据 `orig_body_count + bitmap` 重建 body 序列，未发送位置补0（或补默认值）。

3. 参数
- 新增：`-comm_mode sbc`
- 可选：`-sbc_threshold T`（`|x|<=T`也视为可省略）

### 改动落点
- `src/NoximMain.h`：在 `NoximFlit` 增加 `comm_mode/orig_body_count/bitmap_meta`
- `src/NoximProcessingElement.cpp`：`txProcess/rxProcess` 的包级压缩与恢复
- `src/NoximCmdLineParser.cpp`：解析新参数

---

## B. 多值打包（Multi-Value Packing）

### 思路
把多个激活值打进一个 body flit（例如 2x16bit、4x8bit），减少 body flit 数。

### 为什么能减flit
如果当前每个body只装1个值，打包后理论可降到 `1/2` 或 `1/4`。

### 实现方式
1. 发送端
- 在 `nextFlit` 取值后不立即出队，改为按 `pack_factor` 聚合。
- 头flit携带 `pack_factor` 和 `value_bitwidth`。

2. 接收端
- 解包成多个逻辑 body，再按 `src_Neu_id` 映射填充 `receive_data`。

3. 参数
- 新增：`-comm_mode pack`
- 可选：`-pack_factor 2|4`，`-pack_bit 8|16`

### 备注
你现有 `data0/data1/data2/combine_mode` 已有雏形，可重构成更通用的打包模式。

---

## C. 块级Top-K传输（BTK, Block Top-K）

### 思路
每个包/每个块只发送幅值最大的K个激活，其余直接置0。

### 为什么能减flit
每块固定从 `N` 减到 `K`，压缩比可控（`K/N`）。

### 实现方式
1. 发送端
- 对 body 值按绝对值排序选 top-K。
- 头flit携带 `K` 与位置索引。

2. 接收端
- 先恢复全0块，再把K个位置填入接收值。

3. 参数
- 新增：`-comm_mode topk`
- 可选：`-topk K`、`-topk_block N`、按层配置文件

### 风险
有损，需要联合精度评估（`output.txt` + accuracy）。

---

## D. Delta+RLE时序压缩（DRLE）

### 思路
按包内序列做差分：发送 `x0` + `delta_i`。对 `delta=0` 或重复小delta做RLE。

### 为什么能减flit
如果数据平滑，相邻值差小且重复率高，可显著降低码流长度。

### 实现方式
1. 发送端
- 计算 `delta_i = x_i - x_{i-1}`。
- 小delta用短码，连续0差分用 `(run_len)` token。
- 超范围值回退 raw token。

2. 接收端
- 按 token 流解码恢复原序列。

3. 参数
- 新增：`-comm_mode drle`
- 可选：`-delta_bit B`、`-rle_max R`

---

## E. 预测残差门控（PEG）

### 思路
收发两端维护同一个轻量预测器（如上一个值/均值）。仅发送“残差超过阈值”的位置和值。

### 为什么能减flit
当预测有效时，多数位置无需发送，收端用预测值重建。

### 实现方式
1. 发送端
- 预测 `x_hat`，若 `|x-x_hat|<=T` 则不发，仅记录跳过。
- 否则发 `(idx, residual)`。

2. 接收端
- 用同样预测器先生成 `x_hat`，收到残差再修正。

3. 参数
- 新增：`-comm_mode peg`
- 可选：`-peg_th T`、`-peg_pred last|mean`

### 风险
预测器必须收发严格一致，否则误差会漂移。

---

## F. 多播目的合并（Multicast Replication）

### 思路
同一源激活若要发给多个目的PE，发一次包，路由器/链路处分叉复制，而不是源端重复发多份。

### 为什么能减flit
降低全网总传输 flit（尤其是第一层/分支层的一对多广播）。

### 实现方式
1. 发送端
- 头flit携带 destination mask（或目的列表）。

2. 网络侧（Router）
- 解析 mask，多端口复制转发。

3. 接收端
- 与普通包处理一致。

### 风险
需要改路由器与仲裁逻辑，工程量最大。

---

## 4. 建议落地顺序

1. **A 稀疏位图压缩（SBC）**
- 收益高、实现中等、与现有“按包恢复”机制最匹配。

2. **B 多值打包（pack）**
- 无需复杂数学，主要是编码/解码协议扩展，收益稳定。

3. **C Top-K**
- 最容易形成“可调压缩率”，但需精度评估。

---

## 5. 命令行与模式管理建议

既然你已经明确“通信近似模式互斥”，建议统一成单入口：
- `-comm_mode exact|threshold|allzero|abdtr|droptrunc|sbc|pack|topk|drle|peg`

解析时只保留一个模式源，避免多个布尔开关组合造成歧义。

---

## 6. 评估指标建议

每个新方案固定输出以下指标用于横向比较：
- `Total received flits`
- `Total simulation time`
- `Global average delay`
- `Total energy`
- `accuracy`（最终输出分类）

建议统一脚本跑：
- baseline exact
- 方案X（多档参数）
- 自动汇总到CSV

---

## 7. 一句话总结

如果优先追求“少改动但能显著降flit”，建议先做 **SBC（位图稀疏压缩）**；如果追求“稳定压缩比”，优先做 **pack（多值打包）**。

---

## 8. 重写版：严格去重后的通信+计算协同方案

> 先做声明：以下方案**不再使用**“阈值裁剪/固定间隔丢包/RLE/截断拼包”这几类机制，避免与当前项目已有 `isapprox / allzero / ABDTR / drop_trunc / SAP-RLE` 重合。  
> 数据前提仍是你当前的 `int16` 激活与权重流。
> 另外严格遵循当前项目执行语义：**每一轮必须收齐后再计算**，不存在边收边算。下面所有协同方案都采用“分阶段/分轮次”的 barrier 流程。

### 8.1 PRFT：Progressive Residual Feedback Transmission（渐进残差回传）

### 核心机制
1. 发送端先发 `base` 包（粗表达），再把 `residual` 暂存，不立即发。  
2. 接收端在**收齐本层 base 数据后**，执行第 1 轮计算得到临时结果。  
3. 若该层输出不确定（例如 margin 小、top2 接近），再回发 `REFINE_REQ` 请求对应 residual。  
4. 只有被请求的 residual 才发送；接收端收齐 residual 后执行第 2 轮增量修正（或重算）。

### 与现有方案的本质区别
- 不是“发送前直接近似掉”，而是“先粗发、按需补发”。  
- 决策点在接收端的**计算不确定性**，不是发送端固定阈值。

### 为什么能和计算协同
- 第一轮“收齐 base 后计算”完成后，仅在必要时触发第二轮通信与精算。  
- 这天然是“通信预算”和“计算预算”联动。

### 硬件与实现可行性
- 只需新增一种轻量控制包 `REFINE_REQ`，不要求改 router 算法。  
- 需要增加分轮次状态机（Round-1/Round-2），但不要求边收边算流水。
- python 端易仿真：先跑 base 轮次，再按不确定样本/神经元补 residual 轮次。

---

### 8.2 DDES：Destination-Dependent Edge Sparsification（目的端相关边稀疏化）

### 核心机制
1. 离线计算每条“源组 -> 目的组”连接边的重要度（基于该边对应权重块范数/敏感度）。  
2. 运行时根据拥塞等级，限制每个源组仅向“高重要度目的组子集”发送。  
3. 未发送的边在接收端直接“跳 MAC”，不是收到后置零。

### 与现有方案的本质区别
- 现有方案几乎都在“值域上做近似”。  
- DDES是在“连接边（edge）维度”做选择，减少的是**目的PE扇出数**与包数量。

### 为什么能和计算协同
- 少发给某些目的组，就等价于目的组少做一批输入通道 MAC。  
- 通信削减和计算削减一一对应，便于能耗建模。

### 审稿说服点
- 可解释性强：不是随机drop，而是“基于连接重要度的结构化抑制”。  
- 易给 ablation：不同拥塞等级对应不同 edge 保留率。

---

### 8.3 BECI：Bounded-Error Certified Inference（有界误差认证的轮次终止）

### 核心机制
1. 把输入贡献按“上界优先级”切成多个 `segment`（可离线定序）。  
2. 每轮先完整接收一个 segment，再计算并更新剩余误差上界 `R`。  
3. 若当前判决在 `±R` 内已不可翻转，则**终止后续 segment 轮次**；否则继续下一轮。

### 与现有方案的本质区别
- 不是“直接丢某些值”，而是基于数学上界做**可证安全的早停**。  
- 近似发生在“停止条件”，不是固定格式压缩。

### 为什么能和计算协同
- 一旦某轮认证通过，后续轮次的通信与 MAC 同时停止。  
- 尤其适合最后几层（FC）或高置信样本。

### 工程落地
- 需要在 HEAD 附带 `segment_id/segment_total`。  
- 接收端新增“轮次控制 + 上界累计 + 终止判定”逻辑。

---

### 8.4 PDCM：Predict-Delta Commit Mask（预测-差分提交掩码）

### 核心机制
1. 收发两端共享一个**确定性预测器**（如上一空间邻域/上一通道均值），先得到预测值 `pred`。  
2. 发送端不再发原值，发 `delta = x - pred` 的提交掩码与少量纠正项。  
3. 接收端先收齐 base（预测项）对应数据后做第 1 轮计算；再对“被提交的位置”在第 2 轮注入 delta 修正。

### 与现有方案的本质区别
- 不是 RLE，也不是按幅值阈值裁剪。  
- 关键是“先预测再提交”，通信对象是“预测误差”，不是原始激活。

### 为什么能和计算协同
- 计算上可拆成“base 轮次 MAC + correction 轮次 MAC”，与当前“收齐再算”模型兼容。  
- delta 只在第 2 轮对少量位置修正，减少无效计算。

### 风险与约束
- 预测器必须严格同步，避免漂移。  
- 但这点在定点流水里是可控的（纯整数、无随机）。

---

## 9. 方案对照：与当前项目已有方案的“去重证明”

| 新方案 | 不同于 isapprox | 不同于 ABDTR | 不同于 drop_trunc | 不同于 SAP-RLE |
|---|---|---|---|---|
| PRFT | 不靠固定阈值丢值 | 不按固定间隔丢flit | 不做位截断拼包 | 不做anchor近邻省略 |
| DDES | 不按值近似 | 不随机/周期丢 | 不做bit截断 | 不依赖序列相似性 |
| BECI | 不裁值 | 不丢flit模板化 | 不改数据格式 | 不做RLE式恢复 |
| PDCM | 不直接阈值裁剪 | 不按interval丢包 | 不做固定截位 | 不做same-sign邻近判断 |

---

## 10. 参数怎么定（不靠拍脑袋）

### PRFT
- 参数：`base_bitwidth`、`refine_trigger`（如 top1-top2 margin）。  
- 选法：在 Python 上扫 `(base_bitwidth, margin)`，画 `flit-accuracy` Pareto。

### DDES
- 参数：每层 edge 保留比例 `r_l`。  
- 选法：按层敏感度分配预算（敏感层保留高、鲁棒层保留低）。

### BECI
- 参数：认证阈值 `tau_cert`、排序模板。  
- 选法：保证误差上界不超过该层量化噪声预算。

### PDCM
- 参数：预测器类型、提交率上限。  
- 选法：以“预测残差稀疏度”最大化为目标，而不是幅值阈值最小化。

---

## 11. 建议主线（重写后）

1. **首选：PRFT**  
原因：通信与计算协同最直接，且与现有机制重合最少。

2. **次选：DDES**  
原因：结构化、可解释、容易做论文图（edge保留率 vs 准确率/流量）。

3. **再做：BECI**  
原因：理论最强（可证上界），但实现复杂度最高。

---

## 12. 2026-05-12 更新：无重传约束下的新增方案（层间特征驱动）

> 你的关键约束我这里固定下来：  
> 1) 面向 CNN 层间通信；  
> 2) **基本不存在重传/补发**（单程发送、一次恢复）；  
> 3) 继续遵循“收齐后计算”的执行模型；  
> 4) 近似必须利用层间数据特征，而不是纯随机丢弃。

### 12.1 先约束现有候选

- `PRFT`（需要 `REFINE_REQ` 的二阶段补发）在“无重传”硬约束下不作为主方案。
- 后续主推方案全部采用**一次发送可解码**：包到达后即可恢复，不依赖回传请求。

---

### 12.2 L-ABF：Layer-Adaptive Block-Float（分层自适应块浮点传输）

### 核心思想
- 按 layer 的激活动态范围，把一个 packet 的数据块编码为：
  - 共享移位量/指数 `s`
  - 每个值的低位 mantissa（例如 6/8/10 bit）
- 接收端一次性左移恢复到 `int16` 域，随后按现有流程计算。

### 为什么符合“层间特征驱动”
- CNN 不同层动态范围差异显著：早层范围大、深层范围更集中。  
- `s` 和 mantissa 位宽由**每层统计值**（如 P99 幅值、方差）离线确定，而不是统一阈值。

### 与现有方案区别
- 不是阈值置零（区别 `isapprox`）；  
- 不是间隔丢 flit（区别 `ABDTR`）；  
- 不是 RLE/近邻省略（区别 `SAP-RLE`）。

### 通信+计算协同点
- mantissa 位宽下降后，可直接映射到低位宽 MAC 路径，计算能耗同步下降。

---

### 12.3 DDES-NR：Destination-Dependent Edge Sparsification（无重传版）

### 核心思想
- 离线给每层“源组->目的组”边打重要度分数。  
- 运行时按预设拥塞档位，发送端只向高分目的组发包。  
- 未发送边在接收端按协议直接置 0 并跳过 MAC。

### 为什么符合“层间特征驱动”
- 本质利用的是层的**连接结构冗余**（不是值域阈值）。  
- 对于通道冗余高、扇出大的层，收益更大。

### 无重传保证
- 每个周期的发送集合由预先规则决定，不需要 nack/补发。

---

### 12.4 BDI-ACT：Base-Delta Immediate for Activations（激活值基-差分即刻编码）

### 核心思想
- 每个包选一个 `base`（如首个非零值或块均值整数化）。  
- 后续值发送 `delta`：  
  - 若在 `k` bit 可表示范围内，发短 delta；
  - 超范围发 `escape + raw16`。  
- 接收端一次解码，无需重传。

### 为什么符合“层间特征驱动”
- 卷积层输出在映射顺序上往往局部平滑，低动态差值比例高。  
- 池化后平滑性更强，delta 编码收益更明显。

### 与现有方案区别
- 不丢值、不按阈值近似；  
- 每个非零值都有表示（delta 或 escape raw），属于“有损可控/可近无损”编码路线。

---

### 12.5 CSMR：Channel-Shared Multi-Representative（通道共享代表值编码）

### 核心思想
- 对每层离线聚类得到少量代表值（codebook），按通道组共享。  
- 发送端传 index（低比特）而非原始 `int16` 值；接收端查表恢复代表值。

### 为什么符合“层间特征驱动”
- 你的数据是定点 `int16`，且 ReLU 后分布常呈“稀疏+长尾”，聚类代表值稳定。  
- 每层 codebook 可不同，直接反映层特性。

### 无重传保证
- index 一次传输即可解码，包内不需要反馈机制。

### 通信+计算协同点
- 代表值可约束为硬件友好集合（如移位可实现值），进一步降低 MAC 成本。

---

### 12.6 不同层推荐模式（面向 VGG/LeNet 类 CNN）

| 层类型 | 典型特征 | 建议主模式 | 备注 |
|---|---|---|---|
| 早期 Conv（靠近输入） | 细节多、范围大、稀疏度低 | `L-ABF`（mantissa 较高） | 避免过激近似 |
| 中期 Conv | 局部平滑增强、冗余提升 | `BDI-ACT` 或 `DDES-NR` | 优先降 flit |
| Pool 后层 | 分布更集中 | `L-ABF`（更低位宽） | 收益稳定 |
| 末端 FC / 分类层 | 对准确率敏感 | 保守 `L-ABF` 或轻量 `CSMR` | 精度优先 |

---

### 12.7 参数确定（无重传版）

1. **每层统计**：`sparsity / P95-P99 / 局部差分分布 / 通道相似度`。  
2. **每层模式分配**：从 `{L-ABF, DDES-NR, BDI-ACT, CSMR}` 选 1 个主模式。  
3. **一次发送约束检查**：确保任意模式都能“包内一次解码”，禁用补发路径。  
4. **离线搜索目标**：最小化 `flits + λ*accuracy_drop`，并记录 per-layer 配置表。

---

### 12.8 面向你当前项目的实施优先级（无重传）

1. **先做 `BDI-ACT`**  
- 改动集中在 `txProcess/rxProcess` 编解码，易验证，不依赖 router 修改。

2. **再做 `L-ABF`**  
- 需要每层 profile 文件和按块共享 scale，但硬件解释力强。

3. **最后做 `DDES-NR` / `CSMR`**  
- 论文新意更强，但配置与验证工作量更大。

---

## 13. 2026-05-12 补充：针对“Top-M不可一次读出 + 现有方案重合”的新候选

> 这一节专门回应两点质疑：  
> 1) 不能依赖“6个数多次读取/排序”的复杂选择器；  
> 2) 不能与已有“预测残差死区类方案”高度重合。  
> 目标是保持一次发送可解码、硬件实现可落地，并尽可能提高流量收益。

### 13.1 GSPM：Guarded Sparse Packet + Mean（带保护的稀疏包+均值恢复）

### 核心机制
1. 以每组 `N`（默认可取6）为基本处理单元，发送端单次扫描维护：  
`k_sig`（显著值个数）、`sum_small`（小值和）、`max_abs`、`mask_sig`。
2. 根据统计值选择4种模式（写入 HEAD）：  
`Z`：`k_sig=0`，全零包（无body）；  
`E`：重要值过多或峰值过大，退回精确发送（保护精度）；  
`S`：只发显著值+位置掩码，小值恢复为0；  
`M`：只发显著值+位置掩码+1个小值代表（如均值），小值恢复为该代表值。
3. 接收端按模式一次恢复，不需要重传。

### 为什么针对你的批评有效
- 不需要 Top-M 的“多轮比较/回读”，单次扫描即可完成模式决策。  
- 当“6个都重要”时会自动走 `E` 模式，不强行丢值。  
- 对全零包天然优化（`Z` 模式），优于“只做选大值”的策略。

### 与已有方案差异
- 不是固定间隔丢 flit（区别 ABDTR）。  
- 不是纯阈值归并单值（区别 isapprox/FAS 的单值映射）。  
- 不是 drop_trunc 的位截断拼包。  
- 不是 SAP-RLE 的同号近邻省略。

### 硬件可实现性
- 发送端：比较器+累加器+掩码寄存器+简单状态机。  
- 接收端：按 mode 的固定恢复路径。  
- 不需要全局排序器和复杂回看缓存。

---

### 13.2 PEGS：Peak-Energy Gated Skip（峰值-能量双门控整包跳过）

### 核心机制
1. 每包单次扫描计算两个指标：`max_abs` 与 `sum_abs`。  
2. 双阈值门控：  
若 `max_abs < T_peak` 且 `sum_abs < T_energy`，整包跳过（SKIP）；  
否则进入精确路径或 GSPM 路径。
3. 接收端对 SKIP 包按协议直接恢复为全零或层内默认值。

### 设计动机
- 仅凭单阈值经常误杀“有单个关键峰值”的包。  
- 双门控可保留“尖峰但稀疏”的关键信息，同时跳过弱能量包。

### 与已有方案差异
- 不是“按位置/间隔丢包”，而是“按包内容统计做决策”。  
- 不是预测残差类方案，不依赖预测器同步。  
- 与 FAS 的逐值阈值不同，PEGS 是包级门控。

### 硬件可实现性
- 只需 `abs`、`max`、`sum` 与比较器，数据路径简单。  
- 和现有 HEAD 元数据机制兼容，易集成。

---

### 13.3 OAP：Order-Aware Packetization（顺序感知打包）

### 核心机制
1. 不改数值，只改同一目的PE的装包顺序。  
2. 离线在候选顺序中选每层模板（如 channel-major / row-major / block-major），目标是提升包内相似性或稀疏聚集度。  
3. 在线按模板取值发包，再叠加任意近似编码（如 SAP-RLE 或 GSPM）。

### 为什么值得做
- 你当前“展平后分组”会导致跨通道混组，破坏可压缩性。  
- OAP先提升“包内可压缩结构”，再压缩，通常比直接加大阈值更有效。

### 与已有方案差异
- OAP本身不做近似，属于“近似前处理/数据重排优化”。  
- 与 BDI-ACT、RLE、FAS 均可正交叠加。

### 硬件可实现性
- 在线无需复杂逻辑，仅按预定义索引顺序取数。  
- 主要成本在离线模板搜索，硬件增量很小。

---

### 13.4 三方案协同建议（修订版主线）

1. 先做 `OAP`：提升可压缩结构（低风险、低实现成本）。  
2. 再做 `PEGS`：包级门控，直接减少“弱贡献包”通信。  
3. 最后做 `GSPM`：在保留精度护栏下细化稀疏编码。  

> 该顺序的好处：每一步都能独立 ablation，且对审稿人“是否只是已有方案变体”的质疑更容易回应。

---

### 13.5 2026-05-12 补充：SAP-RLE + zero-skip 组合模式（仅分析，不改代码）

### 背景动机
- 当前 `is_sap_rle` 路径本质是“锚点差值省略”，并不等价于“独立跳0”。  
- 在 ReLU 场景中，零值本身是最稳定、最容易无损压缩的信息源；若不单独利用，会限制压缩上限。  
- 因此考虑组合策略：先做 zero-skip，再对剩余非零序列做 SAP-RLE。

### 组合机制（一次发送可解码）
1. **Step-A: zero-skip（无损）**  
   - 对 packet 内 `BODY`，把 `data==0` 的位置记录到 HEAD（位置+源ID），并从 BODY 删除。  
2. **Step-B: SAP-RLE（有损/可调）**  
   - 对保留下来的非零 BODY 序列再做 anchor 规则：`same_sign && |cur-anchor|<=delta` 则省略。  
3. **接收端恢复顺序**  
   - 先按 SAP-RLE 元信息恢复近似项；  
   - 再按 zero-skip 元信息把 0 插回对应位置；  
   - 最终得到完整序列用于后续计算。

### 与现有单一 SAP-RLE 的预期差异
- 对稀疏层（零值多）应显著提升压缩率，因为先拿到“确定收益”的无损压缩。  
- 对 pool 后零值少的层，增益有限，主要仍依赖 SAP-RLE 本体。  
- 总体收益取决于“零占比 + 非零局部相似度”的乘积，不是只看其中一项。

### 审稿表达建议
- 这不是简单叠加两个旧策略，而是分阶段利用两类统计结构：  
  1) 稀疏结构（zero-skip，无损）；  
  2) 局部相似结构（SAP-RLE，有损可调）。  
- 建议报告三组对照：`zero-skip only`、`SAP-RLE only`、`zero-skip + SAP-RLE`，突出组合增益是否超过单项线性叠加。

### 风险与边界
- HEAD 元信息增长会抵消一部分收益，尤其在“零值分散、非零不相似”的层。  
- 若恢复顺序处理不当，可能导致 src/position 对齐错误（实现时需严格定义恢复优先级）。  
- 该方案在“全零包比例高、且非零序列仍有相似性”的模型/层中最有优势。

### 13.5.1 控制字段建议（固定每包 `n=6`）

> 目标：固定“逻辑数据包大小=6个位置”，近似后只改变 BODY 个数。  
> 这样 Python/RTL 都可用同一协议建模，字段大小可提前定死。

建议在 HEAD 中新增如下字段（近似相关部分）：

1. `mode`（2 bit）  
   - `00`: exact（不近似）  
   - `01`: zero-skip only  
   - `10`: zero-skip + SAP-RLE  
   - `11`: 预留

2. `valid_len`（3 bit）  
   - 当前逻辑包有效位置数，范围 `1..6`（末包不足6时使用）。

3. `zero_mask`（6 bit）  
   - `bit_i=1` 表示第 `i` 个位置原始值为0（被 zero-skip 删除）。

4. `keep_mask`（6 bit）  
   - `bit_i=1` 表示第 `i` 个位置的值在 BODY 中显式发送。  
   - `bit_i=0` 且 `zero_mask_i=0` 的位置，表示由 SAP-RLE 恢复。

5. `delta` 或 `delta_id`（建议 8 bit 或小查表索引）  
   - SAP-RLE 的阈值参数。  
   - 若每层固定，也可不放在每包 HEAD，只在层配置表里给出。

可选冗余校验字段：
- `body_cnt`（3 bit）  
  - 期望等于 `popcount(keep_mask[1:valid_len])`，用于调试与一致性检查。

约束关系（解码前应检查）：
- `zero_mask & keep_mask == 0`  
- `popcount(keep_mask[1:valid_len]) == BODY数量`  
- `valid_len <= 6`

---

### 13.5.2 六个具体数值的完整压缩/解压示例

设一个逻辑包 `n=6`，原始值（按位置1..6）为：

`x = [0, 120, 122, 0, 121, 400]`，并设 `delta=4`。

#### 压缩端步骤

1. **zero-skip（先做无损0跳过）**  
   - 0 出现在位置 `1`、`4`  
   - `zero_mask = [1,0,0,1,0,0]`（写成二进制可记为 `100100`）  
   - 删除后候选序列（保留原位置索引）：  
     - `(p2,120), (p3,122), (p5,121), (p6,400)`

2. **SAP-RLE（对剩余非零序列）**  
   - anchor 初始化为第一个候选 `(p2,120)`，该点必须保留。  
   - `(p3,122)`：同号且 `|122-120|=2 <= 4`，省略。  
   - `(p5,121)`：同号且 `|121-120|=1 <= 4`，省略。  
   - `(p6,400)`：`|400-120|=280 > 4`，保留并更新 anchor。  
   - 因此 `keep_mask = [0,1,0,0,0,1]`（二进制 `010001`）  
   - BODY 只发送 2 个值：`[120, 400]`（按位置升序对应 `p2,p6`）。

3. **HEAD 字段填充（示例）**  
   - `mode = 10`（zero-skip + SAP-RLE）  
   - `valid_len = 6`  
   - `zero_mask = 100100`  
   - `keep_mask = 010001`  
   - `delta = 4`  
   - `body_cnt = 2`（可选）

#### 解压端步骤

1. 读取 HEAD，得到 `valid_len/zero_mask/keep_mask/delta`。  
2. 初始化 `y[1..6]=UNK`。  
3. 按 `zero_mask` 先放回0：`y1=0, y4=0`。  
4. 读取 BODY，按 `keep_mask=1` 的位置升序填入：  
   - `y2=120, y6=400`。  
5. 其余位置为 SAP-RLE 省略位（`zero_mask=0 && keep_mask=0`）：位置 `3`、`5`。  
   - 按恢复规则“优先取最近左侧有效值”：  
     - `y3 <- y2 = 120`  
     - `y5 <- y3 (或 y2) = 120`  
6. 得到恢复结果：  
   - `y = [0, 120, 120, 0, 120, 400]`

#### 本例压缩收益

- 原始值数：6  
- 发送 BODY 值数：2  
- 仅按数据值计压缩率：`(6-2)/6 = 66.7%`  

注意：实际 flit 级收益还要扣除 HEAD 控制字段开销，因此系统总压缩率会低于 66.7%。

---

## 14. 2026-05-13 更新：三套可投稿的新方案（无重传/无补传、一次发送可恢复）

### 14.1 约束与目标（本节统一前提）

- 更新时间：`2026-05-13`
- 数据类型：层间激活为 `int16`（非浮点）。
- 通信约束：不允许重传、不允许补传；每个包一次发送完成。
- 计算约束：保持当前项目语义，仍是“接收端收齐后再计算”。
- 目标：在不改变 NoC 路由框架的前提下，减少 `Total received flits`，同时控制精度损失。

---

### 14.2 EA-SAP：Edge-Aware SAP-RLE（边界感知的 SAP-RLE）

### 核心思想

- 不是对所有值统一做 SAP-RLE，而是先做“边界检测”。
- 对局部平滑段允许 SAP-RLE 省略，对边界突变点强制精确发送。
- 利用 CNN 激活“局部平滑 + 边界突变”的结构特征，而不是纯幅值阈值。

### 发送端流程（包内按顺序单次扫描）

1. 固定逻辑包长 `n`（例如 6）。
2. 第一个有效 `BODY` 作为 anchor，必发。
3. 对第 `i` 个值 `x_i` 计算：
   - 一阶差分：`d1 = |x_i - x_{i-1}|`
   - 二阶差分：`d2 = ||x_i - x_{i-1}| - |x_{i-1} - x_{i-2}||`（`i>=3` 时）
4. 若 `d1 > T_edge1` 或 `d2 > T_edge2`，将该位置标记为边界点并强制发送。
5. 非边界点再执行 SAP-RLE 判定：
   - `same_sign && |x_i-anchor|<=delta` 时可省略；
   - 否则发送并更新 `anchor=x_i`。
6. 在 `HEAD` 写入边界掩码与省略掩码，`BODY` 仅保留实际发送值。

### 接收端恢复流程

1. 读取 `HEAD` 中 `valid_len / edge_mask / keep_mask / delta`。
2. 先按 `keep_mask` 放入显式发送值。
3. 对 `edge_mask=1` 且 `keep_mask=0` 的位置，按协议置为“右邻优先或左邻优先”的保守恢复值（建议右邻优先，避免边界向前拖影）。
4. 对普通 SAP-RLE 省略位按原 SAP-RLE 规则恢复。

### HEAD 控制字段建议

- `mode`（2~3 bit）：标记 EA-SAP。
- `valid_len`（3 bit，`n<=6` 时足够）。
- `edge_mask`（`n` bit）。
- `keep_mask`（`n` bit）。
- `delta_id`（3~4 bit，索引离线表而不是每包发完整 delta）。

### 为什么有创新性

- 现有 SAP-RLE 只看 anchor 差值，不区分“边界”和“平滑段”。
- EA-SAP 把“边界保护”显式编码到通信协议里，是结构先验驱动，而非纯阈值近似。

### 硬件代价（粗估）

- 发送端新增：2 个减法器、2 个绝对值、2 个比较器、少量状态寄存器。
- 接收端新增：一个基于 `edge_mask` 的恢复分支选择器。
- 不需要排序网络，不需要反馈链路。

---

### 14.3 LVBQ：Local-Variance Bounded Quantization（局部方差约束量化）

### 核心思想

- 以“组内统计”决定编码模式，而不是全层一个固定阈值。
- 每组 `n` 个值（默认 6）只允许从有限模式集中选 1 种，保证硬件固定时序。
- 模式示例：`EXACT16 / Q8 / Q4 / ZERO_SKIP_ONLY`。

### 发送端流程

1. 单次扫描得到组内统计：
   - `min_v, max_v`
   - `range = max_v - min_v`
   - `sum_abs`（可选）
2. 用固定门限分档选模式：
   - `range <= R1` 选 `Q4`
   - `R1 < range <= R2` 选 `Q8`
   - `range > R2` 选 `EXACT16`
   - 若全组为 0 则直接 `ZERO_SKIP_ONLY`
3. 对应模式执行打包：
   - `Q4/Q8` 模式下将每值映射到共享 `base + scale` 格式；
   - `EXACT16` 保留原格式；
   - `ZERO_SKIP_ONLY` 只发控制信息。
4. 在 `HEAD` 写模式与量化参数索引。

### 接收端流程

1. 按 `mode` 分支解码。
2. `Q4/Q8` 模式下使用 `base, scale` 恢复到 `int16`。
3. `ZERO_SKIP_ONLY` 直接恢复全 0 组。

### HEAD 控制字段建议

- `mode`（2 bit，可扩展到 3 bit）。
- `valid_len`（3 bit）。
- `qparam_id`（4~6 bit，查每层小表得到 `base/scale`）。
- `zero_mask`（`n` bit，可选；在 `Q` 模式中用于跳 0）。

### 与项目现有方案差异

- 不是 FAS 那种“按值是否低于阈值置零”的映射。
- 不是 drop/trunc 的位截断拼合路径。
- 不是 SAP-RLE 的序列相似省略逻辑。

### 创新点定位

- “组内统计驱动的离散模式选择”是关键。
- 不做复杂在线优化，仍保持一次扫描即可决策，便于 Verilog 落地。

### 硬件代价（粗估）

- 发送端新增：`min/max` 跟踪器、比较器、固定查表。
- 接收端新增：小型反量化单元（移位+乘法可近似为移位加法）。
- 控制复杂度低于通用可变长熵编码。

---

### 14.4 DBB：Deterministic Bit-Budget（确定性比特预算）

### 核心思想

- 每个逻辑包预先给定“最大可发送 bit/flit 预算”，发送端必须在预算内完成编码。
- 不是“压到哪算哪”，而是“固定上界”，直接绑定 NoC 延迟上界。
- 重要度估计不采用排序，仅用分档规则，避免高硬件开销。

### 发送端流程

1. 给每个值计算简化重要度分数：
   - `score = a*|x| + b*|x-x_prev|`
2. 通过 2~3 级固定阈值把值分到：
   - `HIGH`（必须精确）
   - `MID`（低位宽）
   - `LOW`（置零或共享代表值）
3. 预算检查：
   - 若超预算，优先降级 `MID -> LOW`；
   - 若仍超预算，再对 `HIGH` 中最低档做 `Q8`（不直接丢）。
4. 输出固定格式包，`HEAD` 记录每位置类别或类别掩码。

### 接收端流程

1. 按类别掩码恢复：
   - `HIGH` 直接取原值；
   - `MID` 反量化；
   - `LOW` 恢复 0 或组代表值。
2. 无需任何反馈或后续补包。

### HEAD 控制字段建议

- `mode`（DBB）。
- `class_mask_high`（`n` bit）。
- `class_mask_mid`（`n` bit）。
- `repr_id`（代表值索引，可选）。
- `budget_id`（对应层的预算档位）。

### 创新点定位

- 传统方案通常优化平均压缩率，DBB 直接给“每包通信上界”。
- 对 NoC 论文很有利，因为可同时给出：
  - 准确率-流量权衡；
  - 最坏延迟约束（deterministic upper bound）。

### 硬件代价（粗估）

- 发送端：绝对值、差分、分档比较器、预算计数器。
- 接收端：固定类别解码，多路选择器即可。

---

### 14.5 参数确定与审稿说服策略（三方案共用）

### 离线定参流程

1. 对每层激活统计：
   - 零占比
   - 幅值分位数（P50/P90/P99）
   - 一阶差分分布、二阶差分分布
2. 用小验证集扫参数网格，目标函数：
   - `J = flit_ratio + λ * acc_drop`
3. 按层输出配置表：
   - EA-SAP：`T_edge1, T_edge2, delta_id`
   - LVBQ：`R1, R2, qparam_id`
   - DBB：`a, b, budget_id, class_thresholds`

### 审稿人常见质疑与回应要点

1. 质疑：只是旧方案改名字。  
回应：给出“机制差异矩阵”，明确不是阈值置零、不是间隔丢包、不是 RLE 复刻。
2. 质疑：硬件不可实现。  
回应：给组合逻辑路径、比较器数量、关键时序深度估计。
3. 质疑：只在单模型有效。  
回应：至少在 `LeNet5 + VGG16-CIFAR10` 报告每层统计与统一流程。

---

### 14.6 与当前 cnn-noxim 对接建议（最小改动）

1. 只在 `txProcess` 增加新 `mode` 分支，不改原有分支行为。
2. `rxProcess` 复用现有“HEAD 记录元信息，TAIL 到达后整包恢复”框架。
3. 命令行新增独立开关，并保持与现有近似模式互斥。
4. 先实现“仅统计不生效”的 dry-run 模式，验证元信息一致性，再启用真实近似。

---

### 14.7 本节结论

- 若你优先“易实现 + 可解释 + 与现有逻辑兼容”，建议先做 `EA-SAP`。  
- 若你优先“固定协议 + 易做 Python 精度对齐”，建议先做 `LVBQ`。  
- 若你优先“通信延迟上界可控、论文叙事强”，建议先做 `DBB`。

---

## 15. 2026-05-13 继续深化：更强差异化的 4 个候选（避免“换汤不换药”）

> 本节只保留满足以下条件的候选：  
> 1) 无重传/无补传；  
> 2) 一次发送可恢复；  
> 3) 和现有 `isapprox / ABDTR / drop_trunc / SAP-RLE / zero-skip` 有明确机制差异；  
> 4) 可在 Verilog 中低成本实现。

### 15.1 CPSA：Cross-Packet Stateful Anchor（跨包状态锚点）

### 核心问题

- 现有 SAP-RLE 的 anchor 在每个包重置。  
- 当一个逻辑连续序列恰好被“包边界”切开时，可压缩性会被边界破坏。

### 核心机制

1. 对同一 `(src_PE, dst_PE, layer, stream_id)` 维护持久 anchor 状态。  
2. 下一个包的第一个 `BODY` 不再强制做新锚点，而是先和“上一包末锚点”比较。  
3. 若差值小于 `delta_cross`，可直接省略第一个值；否则发送并刷新 anchor。  
4. 每 `K` 个包强制插入 `SYNC_ANCHOR`，防止状态漂移。

### 与现有方案差异

- SAP-RLE 是“包内相关性”，CPSA 是“跨包相关性”。  
- 不改近似值域规则，只扩展相关性维度。

### 硬件实现

- 每条活跃流一个小寄存器：`anchor_value + valid + age`。  
- 发送端增加一次跨包比较器；接收端按同状态机同步恢复。  
- 不需要反馈通道。

### 风险控制

- 任何流重置事件（层切换、图像切换）必须清空该流 anchor 状态。  
- 用 `SYNC_ANCHOR` 周期性硬同步避免长期误差传播。

---

### 15.2 WACA：Weight-Aware Contribution Approximation（权重感知贡献近似）

### 核心问题

- FAS/阈值方案只看激活值大小 `|a|`，没看该激活在下一层的“真实贡献权重”。

### 核心机制

1. 离线计算每个输入神经元的敏感度系数：  
   `S_i = sum_j |W_{j,i}|`（或分组版本 `S_{g,i}`）。  
2. 在线发送时计算贡献代理：`C_i = |a_i| * S_i`（`S_i` 可量化为移位系数）。  
3. 按 `C_i` 分档选择发送模式：  
   - `HIGH`: 精确16bit  
   - `MID`: 粗量化（如8bit）  
   - `LOW`: 置零或共享代表值
4. HEAD 记录每位置类别掩码，接收端按类别恢复。

### 与现有方案差异

- 不是仅按激活幅值阈值（value-only），而是按“值 × 结构敏感度”（value × model）。  
- 和 DDES 的“边是否发送”不同，WACA 在“值级别”做重要度判定。

### 硬件实现

- `S_i` 预存为 2 的幂次近似：`1,2,4,8...`，在线乘法退化为移位。  
- 额外开销：绝对值 + 移位 + 比较器。

### 审稿说服点

- 这是“模型感知通信近似”，不是纯数据驱动阈值。  
- 可直接做 ablation：`|a|` vs `|a|*S` 的同 flit 预算精度差异。

---

### 15.3 TP6：Template Packet Coding for 6-Value Group（6值模板编码）

### 核心问题

- 你当前常见逻辑包大小是 6 个有效值，很多包呈“可解释形态”（全零、平台、单尖峰、阶跃）。

### 核心机制

为 `n=6` 预定义少量模板，发送 `template_id + 参数`，不逐值发送：

1. `T0`: all-zero（发 0 个 BODY）  
2. `T1`: plateau（发 1 个值 `v`）  
3. `T2`: one-step（发 `k, v1, v2`）  
4. `T3`: one-spike（发 `idx, v_bg, v_spike`）  
5. `T4`: ramp（发 `v0, slope`）  
6. `T5`: fallback-exact（发 6 个值）

发送端单次计算每个模板的代价（例如 L1 误差或最大误差），选满足误差约束且码长最短的模板。

### 与现有方案差异

- 不是“删某些值”，而是“结构化参数化重建”。  
- 与 RLE/SAP-RLE 的逐点省略机制不同，TP6 是“块模型编码”。

### 硬件实现

- 模板匹配用固定公式，避免排序。  
- 接收端是小状态机 + 参数展开器。  
- 非常适合固定包长场景（你的 6 值逻辑包）。

### 风险控制

- 模板命中率低时退回 `T5`，保证功能不退化。  
- 可按层禁用某些模板（末层只允许 T0/T5）。

---

### 15.4 MPT：Masked Pair Transform（掩码化成对整数变换）

### 核心问题

- 直接逐值编码没有利用“成对相关性”（相邻/同通道对常高度相关）。

### 核心机制

1. 将 6 值分成 3 对：`(x0,x1),(x2,x3),(x4,x5)`。  
2. 对每对做整数可逆变换：  
   - `s = x_a + x_b`  
   - `d = x_a - x_b`
3. 对每对判断：  
   - 若 `|d| <= Td`，仅发送 `s`（`d` 置 0 或低位量化）  
   - 若 `|d| > Td`，发送 `s+d` 完整对或发送 `(s,d)`  
4. HEAD 写每对模式掩码，接收端按模式逆变换恢复。

### 与现有方案差异

- 不是阈值置零，也不是间隔丢包，也不是 anchor 省略。  
- 利用的是“二元相关变换域”稀疏化。

### 硬件实现

- 每对仅需加减法器；逆变换同样是加减与右移。  
- 全整数路径，适配 int16。

### 风险控制

- 对相关性弱的层，可自动退回原值模式。  
- 可把 `Td` 设为层级参数，避免全局一刀切。

---

### 15.5 四方案优先级（从“最容易落地”到“最可能出新意”）

1. `CPSA`：实现代价最低，能直接验证“跨包相关性”是否被浪费。  
2. `WACA`：最有论文说服力（模型感知），建议作为主线。  
3. `TP6`：最契合你的固定 6 值分组与 Python 仿真。  
4. `MPT`：理论新颖度高，但需要更细致的误差分析。

---

### 15.6 建议下一步（只做分析，不改代码）

1. 用你现有 `VGG16_CIFAR10` 的激活 CSV，先离线评估 `WACA` 与 `TP6` 的上限压缩率。  
2. 先看“理论可压缩上限”，再决定先实现哪一个，避免工程投入后收益太小。  
3. 若你要主攻论文创新，我建议主线定为 `WACA + CPSA`（一个模型感知，一个协议感知），叙事更完整。

---

## 16. 2026-05-19 复盘与新增候选（针对“SAP-RLE 未超过 FAS”）

### 16.1 当前方案清单、优劣势与是否继续推进

| 方案 | 当前状态 | 主要优势 | 主要短板 | 是否建议继续 |
|---|---|---|---|---|
| `exact` | 基线 | 精度基准、行为可解释 | flit 最大 | 必须保留 |
| `isapprox(FAS)` | 已实现/可调 `config_sel` | 压缩强、实现简单、参数直观 | 值域偏置明显，精度风险随档位上升 | 继续，作为强基线 |
| `zero-skip` | 已实现 | 无损、硬件最简单 | 只吃到 0 稀疏性，天花板固定 | 继续，作为无损基线 |
| `all-zero-packet` | 已实现 | 对全 0 包有效 | 仅全 0 包收益，覆盖面有限 | 保留，不作为主线 |
| `SAP-RLE` | 已实现 | 不依赖阈值表、协议简单 | 对“跳变序列”不友好，压缩率常低于 FAS | 保留，用于对比 |
| `SAP-RLE_V2` | 已实现 | 补了全 0 包场景 | 和 zero-skip / all-zero 有重叠区 | 保留，用于消融 |
| `ABDTR` | 已实现 | flit 降幅可控 | 近似失真强、恢复对模式敏感 | 仅对比，不主推 |
| `drop/trunc` | 已实现 | 可控位宽压缩 | 组合模式复杂、收益和精度不稳定 | 谨慎，不主推 |

结论：目前“通信压缩能力”上，`FAS(isapprox)` 仍是最强基线之一。要超过它，不能只在“包内相邻相似性”上做文章，必须减少系统性偏差或引入更强的结构先验。

### 16.2 为什么 SAP-RLE 很难直接超过 FAS

1. `SAP-RLE` 依赖包内局部相似，遇到边界/跳变会频繁刷新 anchor。  
2. `FAS` 直接覆盖“0 + 小非0”两部分，受益面更宽。  
3. 当前包有效载荷固定较小（常见 6 值组），RLE 类方法有元信息摊销上限。  
4. 单一全局 `delta` 难适配各层分布差异（前层/后层统计特性不同）。

---

### 16.3 新增方案A：EFDQ（Error-Feedback Deadzone Quantization）

### 核心思想

发送端对每个数据流维护一个误差累积寄存器 `r`：

1. 计算 `u = x + r`。  
2. 若 `|u| <= T`，本次不发（按0处理），并令 `r = u`。  
3. 若 `|u| > T`，发送量化值 `q(u)`（可 8/16bit），并令 `r = u - q(u)`。

这是一种“误差前馈到未来样本”的单向机制，不需要重传，不需要接收端额外状态同步。

### 与现有方案差异

- 不等于 FAS：不是静态阈值直接抹零，而是把被抹掉的误差累积并在后续样本释放。  
- 不等于 SAP-RLE：不依赖相邻值相似，不做 anchor 省略。

### 预期收益

- 在相近压缩率下，较 FAS 更不易产生系统性负偏差。  
- 对最终分类层前的累计误差更友好，精度-流量曲线有机会外移。

### 硬件代价

- 每流新增 1 个寄存器 `r` + 加法器 + 比较器。  
- 纯整数路径，适配 `int16`。

### 风险

- 误差从“当前位置”迁移到“后续位置”，局部空间失真可能上升。  
- 需在 Python 侧做逐层误差热图验证。

---

### 16.4 新增方案B：HFVC-16（Hot-Frequent Value Coding for int16 Activations）

### 核心思想

利用量化后激活“高频离散值”特征：

1. 离线统计每层 top-K 高频非0值（例如 K=8 或 16），生成小 codebook。  
2. 发送时：  
   - 值命中 codebook：发送短码（如 3~4bit + mode）；  
   - 未命中：发送 `escape + raw16`。  
3. 0 值继续走 zero-skip（无损）。

### 与现有方案差异

- 不是按幅值阈值近似（区别 FAS）。  
- 不是按序列相似省略（区别 SAP-RLE）。  
- 是“值分布驱动”的字典编码。

### 预期收益

- 对“离散峰值明显”的层（尤其 ReLU 后中后层）可能取得高于纯阈值法的压缩效率。  
- 方案可做成“无损字典命中 + 有损降级”的两段式。

### 硬件代价

- 比较器阵列（K路）+ 小 LUT。  
- 控制逻辑固定，Verilog 实现可控。

### 风险

- 若输入分布漂移，命中率下降，收益会退化。  
- 需要 per-layer codebook 管理（配置复杂度上升）。

---

### 16.5 新增方案C：MPC-1（First-Moment Preserving Packet Compression）

### 核心思想

在阈值压缩基础上增加“一阶矩守恒”补偿：

1. 先按阈值规则决定 drop 集合（例如低重要度值置0）。  
2. 计算被丢弃值总和 `S_drop`。  
3. 选择一个保留位置 `idx_c`（如幅值最大或首个保留值），发送 `v_idx_c + S_drop`。  
4. 头部记录 `idx_c`（和可选饱和标志），接收端直接恢复。

### 与现有方案差异

- 与 FAS 相比：不是“只置零”，而是补偿掉被置零的总量，降低直流偏差。  
- 与 SAP-RLE 相比：不依赖相邻关系，是包内统计守恒策略。

### 预期收益

- 在接近 FAS 的 flit 数下，可能获得更好的精度保持。  
- 对全连接前层（累加敏感）尤其有价值。

### 硬件代价

- 包内多一个求和器 + 一个索引选择器。  
- 接收端仅需一次加法恢复，无重传。

### 风险

- 补偿值可能导致单点过大，需要 `int16` 饱和保护。  
- 若包内仅剩极少保留值，局部噪声会集中。

---

### 16.6 现阶段建议主线（更新）

1. **优先做 `MPC-1`**：改动小、验证快、最容易直接对标并挑战 FAS。  
2. **第二做 `EFDQ`**：如果目标是“同压缩率下精度更稳”，它比纯阈值更有理论说服力。  
3. **第三做 `HFVC-16`**：当你需要“与阈值/RLE完全不同的编码路线”时，用它做论文差异化补强。

建议统一比较口径：  
`exact`、`FAS(config_sel=0/1/3)`、`SAP-RLE(best delta)`、`MPC-1`、`EFDQ`，固定相同输入与映射参数，报告 flit/cycle/accuracy 三条曲线。
